# loop lessons — good/bad メモリ

運用ルール:
- 毎ループの出口で 1 entry 追記する (GOOD でも BAD でも。「特になし」は禁止 — 小さくても何かある)
- 追記前に既存 entry を Grep し、同一根本原因なら新 entry を作らず「発生回数」を増やして日付を併記する
- **同一根本原因 2 回目 → CLAUDE.md にルールとして昇格**し、entry に「→ CLAUDE.md 昇格済み」とマークする
- 入口で読むのは「一般化ルール」行のみで足りるよう、ルールは指示文として書く

---

## 2026-07-08 st37-privacy-page 仕様書のタスク行に他アプリの慣習（7ロケールplaceholder）が混入→委譲前に対象アプリの実構成を1次確認して成果物定義を是正／法務ページの記載事実を実装コードに限定して過大約束を防止 [GOOD]

- 事象: ST-37仕様行の「`privacy.*` i18n（en/ja 実訳＋**他5 en placeholder**）」は、codex（7ロケール）の慣習表現が計画書へコピーされた混入だった。scriptoriumは`locales/en.json`/`ja.json`の2ロケール構成であることを委譲前に1次確認し、成果物定義を「en/ja実訳のみ」へ是正してから委譲（そのまま渡していたらimplが存在しない5ロケールのファイル新設や質問往復で迷走し得た）。また、プライバシーポリシーの記載事実（生IP非保存=HMAC-SHA256・rate_limits期限剪定・削除PW=PBKDF2復元不可・通知メール=運営者宛のみ・localStorage=言語キーのみ）を全て実装コード（rateLimit.ts/0001_init.sql/notifier.ts/i18n index.ts）でGrep確認してから、委譲プロンプトに「この事実から逸脱・誇張しない」と固定した＝法的文書が実装と乖離する（過大約束・虚偽記載）リスクを設計段階で封じた
- 原因（GOODの機序）: 計画書のタスク行は前例タスクの複製で書かれるため、アプリ横断の慣習（ロケール数・ディレクトリ構成）が対象アプリに合わない形で混入し得る。「タスク前提の事実の1次確認」を成果物定義そのもの（ロケール構成）にも適用した。法務・ポリシー文書は「実装が何をしているか」の宣言であり、文面を書く前に宣言対象の実装を確認しないと乖離がそのまま文書化される
- 一般化ルール (次ループの指示文としてそのまま使える形で): 仕様書のタスク行にあるロケール数・ファイル構成・慣習的成果物（「他5 placeholder」等）は、**対象アプリの実構成（ls/Grep）で委譲前に検証**し、混入していれば是正してから委譲する（計画書は前例複製で書かれるため他アプリ慣習が混入し得る）。プライバシーポリシー・利用規約等の**実装挙動を宣言する文書**は、記載する各事実を実装コードで1次確認したリストとして委譲プロンプトへ固定し「逸脱・誇張しない」と指示する（文書と実装の乖離=過大約束を設計段階で封じる）
- 反映先: loop prompt（1次確認family=仕様書レコードの慣習混入という新相・宣言文書の事実固定）
- 発生回数: 1回目（1次確認family 10回目の実証）

## 2026-07-08 st36-security-headers レビューHigh指摘（Turnstile CSP破壊）を公式docs 1次確認で棄却→実機の実トークン発行が棄却を経験的に裏付け＝不要なCSP緩和ゼロ [GOOD]

- 事象: ST-36レビューR1がFAIL(H2)「connect-srcに`challenges.cloudflare.com`が無くTurnstileが沈黙破壊される」を返した。セッションは修正委譲せずCF公式docs（turnstile/reference/content-security-policy）をWebFetchで1次確認し、**必須ディレクティブはscript-srcとframe-srcのみ・チャレンジ通信はクロスオリジンiframe内=親ページのconnect-src非適用**（Pre-clearanceでも要るのは`'self'`だけ）を確定して棄却。R2で同一レビュアーもTurnstileWidget実コード（親ページfetchなし）を検証して撤回→PASS(C0/H0/M0/L0)。さらに出口実機（pages dev=ヘッダー実適用環境・テストキー焼き込みビルド）で**実Turnstileトークン発行（hidden input実測）と通報POST完全往復**を確認し、棄却が経験的にも正と裏付けた。反射的に採用していれば不要なCSP緩和（connect-src拡大）が恒久化していた
- 原因（GOODの機序）: ①「レビュー指摘の前提事実は1次確認」ルールを、外部プラットフォーム仕様の断定（CSPの適用範囲）へ適用した——CSPのスコープ規則（per-document・iframe内は別文書）はレビュアーの誤認が起きやすい領域 ②棄却で終わらせず「実機で経験的に最終確認する」出口を裁定に組み込んだ（docsが古い/読み違いのリスクを実挙動で閉じる二段構え）③検証環境の選定が正しかった: `wrangler pages dev`はscriptoriumの`_worker.js`ヘッダー注入もcodexの`_headers`も**両方とも実適用する**ため、CSP有効状態での実フロー検証がローカルで成立した
- 一般化ルール (次ループの指示文としてそのまま使える形で): セキュリティポリシー（CSP・CORS・Permissions-Policy）への「Xが足りず機能が壊れる」系レビュー指摘は、修正前に**公式docsでそのディレクティブの適用スコープ（per-document・iframe境界・リダイレクト）を1次確認**し、棄却する場合も**実機で該当機能の完全往復**（トークン発行・POST成功など状態遷移の実測）を出口に含めて経験的に閉じる。CSP/ヘッダー系の実機検証は本番と同じ注入経路が動く環境（pages dev＝`_worker.js`も`_headers`も実適用）で行う——vite devはどちらも適用しないため代表しない
- 反映先: loop prompt（1次確認family=プラットフォーム仕様断定の相・ヘッダー系検証環境の選定）
- 発生回数: 1回目（1次確認family 9回目の実証。2026-07-08計画ループの「外部チェックリスト照合」と同日・別相）

## 2026-07-08 security-checklist チェックリスト依頼を計画化する前に全項目を実コード照合＝実装済みの再実装回避＋構成依存の落とし穴を設計段階で捕捉 [GOOD]

- 事象: ユーザー提供の公開前セキュリティチェックリスト（11節）を「計画に入れて」の依頼に対し、いきなりタスク化せず**3体のExploreで全項目を実コード（server/・recipe-core・codex lib・wrangler/gitignore）に1次照合**した。結果、入力検証・SQLi・レート制限・PBKDF2・Turnstile fail-closed・XSS sink・画像マジックバイト・EXIF除去・シークレット衛生は**実装済み＆テスト済み**と判明し、ギャップ5点（セキュリティ応答ヘッダー・プライバシーポリシー・flagged画像R2残存・pages.dev noindex・履歴スキャン）だけをST-36〜39＋ST-33/34拡張として計画化。照合せずチェックリストを丸ごとタスク化していたら実装済み十数項目を再実装する無駄が出ていた。さらに照合の副産物として**構成依存の落とし穴を設計段階で2つ捕捉**: ①scriptoriumはadvanced mode（`dist/_worker.js`）でPagesの`_headers`が無視される→CSPは`worker.ts`注入が正（codexは純静的で`_headers`可）②両アプリともインラインscriptゼロ→CSP `script-src`に`'unsafe-inline'`不要（`style-src`のみ必要）。CSPは機能を静かに壊すため完了条件に「主要フロー全通しでコンソール違反ゼロ」を必須化
- 原因（GOODの機序）: 「レビュー指摘・タスク前提の事実は委譲前にセッションが1次確認」（CLAUDE.md昇格済みルール）を、**外部提供のチェックリスト**という新しい入力形態へ適用した。チェックリストは「やるべきこと」を列挙するが「既にやってあること」は教えない→実装との突き合わせが必須。突き合わせは同時に「その対策を実現する具体構成（配信形態・ヘッダー注入経路）」を確定させ、机上計画では見えない環境差を炙り出す
- 一般化ルール (次ループの指示文としてそのまま使える形で): 外部提供のチェックリスト/監査項目/要求仕様を計画へ落とすときは、**各項目をまず実コードへ1次照合し「実装済み／ギャップ／要検証」に仕分けてから**タスク化する（丸ごとタスク化＝実装済みの再実装。照合はExplore並列が安い）。セキュリティヘッダー/CSP等の「配信レイヤ」対策を計画するときは、対象アプリのPages配信形態（advanced mode `_worker.js` か 純静的か）を先に確定する——`_worker.js`構成では`_headers`が無視されヘッダーはWorkerコードで注入する必要がある。CSPは機能を静かに壊すため、実装タスクの完了条件に「全主要フロー実機でコンソール違反ゼロ」を必須化する
- 反映先: loop prompt（1次確認family＝外部チェックリストの実コード照合という新しい相・配信形態の設計段階確定）
- 発生回数: 1回目（1次確認ルールfamily 8回目の実証・新相=外部提供チェックリストの突き合わせ）

## 2026-07-08 scriptorium-tagline PR状態を確認せずマージ済みPRのブランチへ追いコミットをプッシュ→mainに届かず [BAD]

- 事象: PR #59作成後、ユーザーのタグライン変更指示をglue修正して`git push`（追跡ブランチ=PRのheadブランチ）した。しかしPRはプッシュの19分前にマージ済みで、コミットはマージ済みブランチの残骸にだけ届き**mainに入らなかった**。ユーザーの「マージ済みだけどどこにプッシュしたの？」で発覚→origin/mainからcherry-pickして別PR（#60）で救済
- 原因: 「PRブランチに追いコミット」の前提=**PRがまだopenである**ことを確認しなかった。push自体は成功するため、エラーは出ず気づけない（成功したpushが「意図した到達先に届いた」を意味しない）
- 一般化ルール (次ループの指示文としてそのまま使える形で): PR作成後にそのブランチへ追いコミットをプッシュするときは、**push前に`gh pr view <N> --json state`でopenを確認**する。MERGEDなら追いコミットせず、origin/mainから新ブランチ→cherry-pick→新PR。ユーザーがマージを自分のタイミングで行うリポジトリ（本リポジトリの運用）では、PR作成〜追い修正の間にマージが挟まる前提で動く
- 反映先: loop prompt（出口処理のPRブランチ運用）
- 発生回数: 1回目

## 2026-07-08 scriptorium-header-brand レビューM指摘（静的レイアウト推定）とユーザー報告（favicon誤表示）の両方を実測で確定してから裁定＝不要修正ゼロ [GOOD]

- 事象: ①review R1のM1「375px幅でtagline nowrapがヘッダー右溢れ（静的推定241+131>コンテンツ311px）」を、修正委譲せず出口実機（pages dev・JA/EN両言語×375/768/1280）で実測→docScrollW=375・横はみ出し0・brand/langSwitcher全ヒットテスト真＝推定が過大（実測brand幅212〜219px）で**棄却・無修正採用**。提案されていたCSS複雑化（min-width:0＋ellipsis 3セレクタ）を回避 ②ユーザー報告「faviconがなぜかcodexのを表示」を、修正前に本番配信実体のcurl+md5をローカルと突き合わせ→「配信は正しい・真因はブラウザのfaviconキャッシュ（scriptorium `vite dev`もport 5173=codexと同一オリジンでキャッシュ共有が主因候補）」と確定→対処をcodex前例と同じ`?v=2`バスター1行に限定
- 原因（GOODの機序）: 「レビュー指摘・障害報告は事実確定してから対処」（1次確認family）を、修正の**採否判定**（指摘の棄却）と**対処の最小化**（キャッシュバスター1行）に適用した。レイアウトの静的幅推定はフォント実寸なしでは過大/過小になりやすく、机上で修正を確定すると不要なCSSが積もる
- 一般化ルール (次ループの指示文としてそのまま使える形で): レビューのレイアウト系指摘（幅・溢れ・重なりの静的推定）は、修正委譲の前に該当ブレークポイントの実測（scrollWidth・elementFromPoint）で成立/不成立を確定し、不成立なら**無修正で棄却を記録**する。「本番の表示がおかしい」系の報告は、配信実体（curl+ハッシュ）とローカル実体の突き合わせで「配信の問題かキャッシュの問題か」を確定してから最小の対処を選ぶ（キャッシュ相ならURLバスターが定石）
- 反映先: loop prompt（1次確認familyの新相=指摘の棄却判定・障害報告のキャッシュ切り分け）
- 発生回数: 1回目（1次確認family 7回目の実証）

## 2026-07-08 scriptorium-s6-moderation SendMessage再開レビュアーが大transcript（≈110k token）で2回連続脱線（ツール呼び出しがテキスト化）→フレッシュレビュアー切替が機能 [BAD→GOOD]

- 事象: S6 Wave2のRound 2を「同一レビュアーへSendMessage」定石（ST-19昇格運用）で依頼したところ、R1完了済みレビュアー（transcript 79k→108k token）が**ツール呼び出しを実行せずテキストとして出力**する形で2回連続停止した（verdict未出力・壊れたinvoke構文=context loss兆候）。安全弁を2回目で適用し、**フレッシュレビュアーをR1結果の要約引き継ぎ付きで新規起動**→一発で完全なR2 verdict（反例検証込み）を得た。なお同ループ内の別レビュアー（R2担当・73k token）へのR3 SendMessageは正常に機能＝閾値は絶対値でなくtranscript肥大の程度問題
- 原因: SendMessage再開は文脈保持が利点だが、transcriptが大きいエージェントの再開は出力構文の崩壊（tool callのテキスト化）を誘発し得る。「文脈保持」の価値は、再開に失敗して往復を浪費した時点で失われる
- 一般化ルール (次ループの指示文としてそのまま使える形で): 完了済みエージェントへのSendMessage再開が「ツール呼び出しのテキスト化・verdict未出力」で失敗したら、**再ナッジは1回まで**。2回目の同型失敗で即フレッシュエージェントへ切替え、前ラウンドの結果（PASS/FAIL・指摘一覧・修正内容）をプロンプトに要約して引き継ぐ（自己完結プロンプトなら文脈損失は実害にならない）。transcriptが10万tokenを超えたエージェントへの再開依頼は最初からフレッシュ起動を検討する
- 反映先: loop prompt（Round N+1のSendMessage運用の境界条件。2回目発生でCLAUDE.md「委譲・レビューの規律」へ昇格）
- 発生回数: 1回目
- 付記GOOD: ①**セッション独立ゲートが同ループでimpl誤報告を2件捕捉**（「postRecipe.ts無変更」報告→実際は1行変更〔委譲時許可済みで実害なし〕・テスト件数の過大自己申告→`vitest list`実測で照合）＋無関係ファイル（codex PaintPicker）の並列時flakyを「単独2回＋全件再実行」で切り分け=独立再実行ルール（CLAUDE.md昇格済み）の実効3例目。**impl委譲の完了条件に「テスト件数は自己申告でなくvitest list実測」を含めると照合が速い** ②**Turnstileの公開テストキー**（sitekey `1x00000000000000000000AA`＋常時passシークレット・`.dev.vars`）で、S4では全面ユーザー引き渡しだった**Turnstile越しフローのローカル実e2e**（実ウィジェット描画→token→実サーバー検証→D1書込）が成立=「環境ギャップは設計段階で検証手段を組み込む」familyの新手段。外部サービスの検証用ダミー資格情報の有無をdesign段階で1次確認する価値がある ③実機ヒットテストは「要素中心のhitOk」だけでなく**「操作の実効範囲」（行端クリックでトグルが起きるか）**まで見る=通報radioの「min-height 44pxは視覚のみ・実効タッチ18px」を検出しglue（labelを行全域化）。44px規律の検証は「見た目の高さ」でなく「その座標のクリックが機能するか」で判定する ④ビルドハッシュが変わる再ビルド後は、worker側HTMLキャッシュ（/r/:id OGP注入300s）が旧アセット参照で残る→**クエリ付きURLでキャッシュキーを回避**が実機検証の定石 ⑤外部プラットフォームの料金・プラン制約はdocsだけでなく**dashboard実挙動が正**（CF Email Sending: docs「検証済み宛先は全プラン無料」vs 実挙動「オンボードがWorkers Paid専用」の乖離をユーザー実測が検出）=「プラットフォーム設定記法の1次確認」familyの料金相

## 2026-07-08 scriptorium-s5-import レビュアーの修正提案自体を1次確認し欠陥を検出（提案regexが`..`素通り）／CORS環境ギャップを設計段階でdevプロキシに折り込みローカル往復e2eを成立 [GOOD]

- 事象: ①S5レビューR1のL1指摘（coverUrl検証強化）でレビュアーが提案した `/^\/img\/[A-Za-z0-9._/-]+$/` は、文字クラスに`.`と`/`を両方含むため肝心の`..`（path traversal）が素通りする欠陥があった。セッションが修正委譲前に提案regexを机上検証（反例`/img/../../x`を当てる）して検出し、`refine((v) => !v.includes("..") && !v.includes("//"))`の明示排除へ変えて委譲。R2で同一レビュアーが実キー形式（postRecipe.ts/feed.ts/seed.mjs）との突き合わせまで行いPASS ②本番CORSがACAO: `coat-codex.com`固定＝localhostから実fetch不可という環境ギャップを、S4コーデック教訓「環境ギャップは設計段階で潰す」の適用としてdesignの1次確認（サーバーACAO実装のRead）で特定し、vite devプロキシ（`/__scriptorium`→wrangler:8788）＋`import.meta.env.DEV`分岐のAPI baseを実装要件に組み込んだ。結果、出口実機検証で「実fetch→実サーバー→実D1/R2→実dataURL化→実Dexie書込→実遷移」のローカル往復e2eが成立（Turnstileの時=全面ユーザー引き渡し、と違いpreview検証が本番経路の大部分を代表できた。残ギャップ=本番ACAO/https/iOS実タッチのみをST-25へ引き渡し）
- 原因（GOODの機序）: ①レビュー指摘の「指摘内容の事実」だけでなく「修正提案そのもの」も1次確認の対象にした（1次確認ルールの新しい相。regex・エスケープ・丸めを含む提案は反例1つの机上検証が安い）②環境ギャップは出口検証で発覚してから対処すると手戻りだが、design段階で特定すれば「検証可能性そのもの」を設計に組み込める
- 一般化ルール (次ループの指示文としてそのまま使える形で): レビュー指摘を修正委譲するとき、レビュアーの修正提案（正規表現・エスケープ・境界値・丸め）はそのまま委譲プロンプトへ貼らず、**セッションが反例を1つ当てる机上検証をしてから**採否・修正して渡す。外部サービスへのfetchを含む新機能のdesignでは、CORS/認証/コーデック等の環境ギャップを1次確認し、「ローカル検証をどう本番経路に近づけるか」（dev専用プロキシ・注入可能なAPI base）を実装要件に含める（検証手段の設計はコードの設計と同時に行う）
- 反映先: loop prompt（1次確認ルールの適用対象を「レビュアーの修正提案」へ拡張・環境ギャップの設計折込み）
- 発生回数: 1回目（1次確認ルールfamily 6回目の実証・新しい相=修正提案の検証）
- 実証: 2026-07-08 PR #55マージ→ユーザーiPhone通しQA（投稿→閲覧→インポート→再編集）が一発通過＝ST-25完了・S5完了。ローカル往復e2e（devプロキシ）が本番経路を正しく代表していたことのE2E実証

## 2026-07-08 cover-jpeg 出力フォーマット指定Web API（canvas.toBlob("image/webp")）のブラウザ対応を設計段階で未確認＝Chromiumプレビュー検証が実機Safariを代表せず、本番投稿が400 [BAD] → CLAUDE.md 昇格済み（実機検証の規律に追記）

- 事象: ST-20/21で投稿cover/thumbをWebP（canvas.toBlob("image/webp", q)）で生成する設計にしたが、**iOS/デスクトップSafariはcanvasのWebPエンコードに非対応**（Safari 26/27でも未対応・caniuse実測）。toBlobは仕様上「非対応形式はPNGへ暗黙フォールバック」のため例外も出ず、実体PNGが`image/webp`詐称Fileで送信され、サーバーの実バイト検査（parseWebPHeader）で400 `invalid cover image`。Chromium系プレビューはWebPエンコード可のため全検証を素通りし、ユーザーのiPhone実投稿で初めて露見（クロップtouch/clipboardと同じ「preview≠実機Safari」ギャップのコーデック版）
- 原因: ①出力フォーマットを指定するWeb API（canvas.toBlob/toDataURL・MediaRecorder mimeType等）は**ベストエフォート仕様＝非対応でもエラーにならず別形式へ静かに縮退**する。対応確認を設計段階でcaniuse等で行わなかった ②送信Fileのtype/拡張子をハードコード詐称（"cover.webp"/"image/webp"）していたため、クライアント側でも実体との乖離を検出できなかった。対処は**常時JPEG化**（全ブラウザ対応・写真coverで予算内・分岐なし1経路＝プレビュー検証が実機を代表するようになる）＋サーバーWebP/JPEG両受理＋File type実blob導出
- 一般化ルール (次ループの指示文としてそのまま使える形で): **出力フォーマット/コーデックを指定するWeb API（canvas.toBlob/toDataURL・MediaRecorder等）を使う設計は、指定形式の全対象ブラウザ対応（特にSafari/WebKit）をcaniuseで設計段階に1次確認する。非対応時は暗黙の別形式フォールバックが起きる前提で、(a)可能なら全ブラウザ共通形式（写真=JPEG）へ寄せて分岐自体を消し、(b)送信メタデータ（File type/拡張子）は宣言でなく実blob.typeから導出する**。バイナリを受けるAPI境界はcontent-type宣言でなく実バイトのマジックバイトで検証する（今回サーバーが正しく弾いたのはこの設計のおかげ＝維持）
- 反映先: CLAUDE.md 実機検証の規律（「preview（Chromium）が実機Safariを代表しない」ファミリーの2回目＝コーデック相を追記昇格）
- 発生回数: 2回目（1回目=2026-07-05 clipboard/WebShare/touch系でCLAUDE.md昇格済み。今回はコーデック/エンコード形式という新しい相を同ルールへ追記）
- 根治実証: 2026-07-08 PR #53マージ→本番デプロイ後、ユーザーiPhone実投稿成功＋レシピ表示GOOD確認＝JPEG化で環境ギャップ解消をE2Eで実証（実投稿ブロッカー3連〔413→validation不明瞭→400 invalid cover〕全クローズ・S4本番開通）

## 2026-07-08 cover-jpeg 前ループの「validationエラー具体化」投資が翌ループの本番障害診断を一撃化した（エラー文言のサーバーガード逐語表示→grep一発で真因行特定） [GOOD]

- 事象: ユーザー報告「レシピの検証に失敗したため公開できませんでした。invalid cover image」に**サーバー側ガードの逐語文字列**が含まれていたため、`grep "invalid cover image"` 一発でpostRecipe.ts:202（parseWebPHeader null経路）を特定、DevTools往復ゼロ・診断開始から真因確定（Safari WebPエンコード非対応のweb 1次確認込み）まで数分で到達した。前ループまでは同じ失敗が汎用文言「レシピの検証に失敗しました」のみでブロッカー化していた（memory: 診断手順(a)DevTools Networkタブ依頼を準備していた）
- 原因: 前ループでPublishError.messageへサーバー応答body.error/検証issueの具体情報を通す改修を入れていたため、ユーザーの自然な報告文にサーバー側の失敗ガード識別子がそのまま乗った。**エラー文言を「ユーザー向け汎用文＋機械的識別子（ガード名/フィールドpath）」の二層にすると、ユーザー報告がそのまま診断入力になる**
- 一般化ルール (次ループの指示文としてそのまま使える形で): ユーザー実機でしか再現しない失敗経路（環境依存・本番限定）を持つ機能は、エラー表示に**サーバー/内部ガードの逐語識別子を必ず含める**（汎用文言だけにしない）。これはUX改善でなく「ユーザー報告を診断入力に変える」観測性投資であり、環境依存バグの往復回数を直接削る。逆に、ユーザー報告に具体識別子が含まれていたら、まず逐語grepで該当ガードを特定してから仮説を立てる（推測先行しない）
- 反映先: loop prompt（環境依存機能のエラー設計指針）
- 発生回数: 1回目

## 2026-07-08 cover-413 クライアント生成物が受信側の上限を「目標」で満たすだけで「保証」していない＝境界超データで受信側が拒否／unit(合成データ全セーフ)が本番の実データ分布を代表しない [BAD]

- 事象: ST-21実投稿でサーバー413（cover>450KB）。`coverComposer`はcoverを「200-400KB目標・q下限0.3」で二分探索していたが、高精細写真だとq=0.3でも450KB超のblobをそのまま返し、サーバーの`COVER_MAX_BYTES=450KB`検査で拒否された（thumbは目標=上限80KBで余裕ゼロ）。ローカルのシード5件は全て偶然上限内（cover最大373KB）だったためunit・render検証を素通り。セッションが**純ノイズ最悪画像で574KB>450を実測再現**して真因を確定→`encodeUnderHardLimit`（q下限0.1＋寸法段階縮小フォールバック）で上限内を保証
- 原因: ①送信側（クライアント生成物）が受信側（サーバー）の**hard上限**を「目標値で近似」しただけで「保証」していなかった（目標400KB<上限450KBの余白頼み・境界超で破綻）②検証データ（シード5件・render用実データ）が**実利用のデータ分布の上側テール**（高精細/大サイズ写真）を代表していなかった＝「検証が実利用を代表しない」系の画像サイズ版 ③ユーザー提案「JSON圧縮で解決」は誤り＝大きいのは既圧縮のWebP画像（gzip再圧縮は効かない）でJSONは既に64KB上限＝小。上限内化は品質/寸法縮小のみが有効、という切り分けが必要だった
- 一般化ルール (次ループの指示文としてそのまま使える形で): **クライアント側で生成し受信側APIへ送るデータ（画像・ファイル）は、受信側のhard上限を「目標値」でなく「保証」する**（二分探索の下限を十分下げる＋それでも超えるなら寸法/内容を縮小するフォールバックで、境界超入力でも必ず上限内に収める。送信側目標＜受信側上限の余白頼みにしない）。**サイズ/境界に関わる検証には、シード/実データの平均値だけでなく上側テール（最大サイズ・最悪圧縮率＝純ノイズ等の合成最悪ケース）を必ず含める**。「圧縮で容量問題を解決」提案には、対象が既圧縮フォーマット（WebP/JPEG/PNG/zip）でないかをまず確認する（既圧縮データのgzip/再圧縮は数%も効かない・削減は品質/寸法/解像度のみ）
- 反映先: loop prompt（実機検証の規律の画像サイズ版＝上側テール検証・生成物は受信側上限を保証）
- 発生回数: 1回目（「検証が実利用を代表しない」系の新しい相＝データ分布の上側テール。CLAUDE.md実機検証の規律に既存の「分岐を変えるデータバリエーション」と同族だが、サイズ/境界の上側テールは新観点）

## 2026-07-08 scriptorium-s4-st21 implの「build exit 0」報告が誤りだった（tsc増分キャッシュ/楽観報告）→セッションは裁定でbuildを独立再実行する [BAD] → CLAUDE.md 昇格済み

- 事象: ST-21 Wave A（publishToScriptorium）でimplが「`npm run build`: exit 0」と報告したが、セッションが裁定で独立再実行すると**実際は exit 1**（`vi.fn` に関数型注釈を付けたため Mock 型が消え `.mock` アクセスが TS2339）。vitest は esbuild で型検査しないため 1402 テストは緑・tsc だけが赤。ST-18 では「完了条件に tsc を入れ忘れ」て型リグレッションが露見、ST-21 では「tsc を入れたのに impl の exit-code 報告が不正確」＝同根（vitest 緑 ≠ tsc 緑）の別相。セッションの独立再実行が捕捉し、`vi.fn<(...) => Promise<void>>()` ジェネリック型付け（Mock 型＋引数型を両立）で修正。修正が `no-unused-vars`（`_` 接頭辞非許容）で再度 lint 赤になったのも独立再実行で検出しジェネリック形へ再修正
- 原因: ①vitest(esbuild/isolatedModules)は型を検査しない ②impl の完了報告は tsc 増分キャッシュ（tsBuildInfo）や楽観で「build 緑」が偽になり得る ③`vi.fn` に明示関数型注釈を付けると Mock 型が失われ `.mock` が使えない（vitest の型付けの罠）
- 一般化ルール (次ループの指示文としてそのまま使える形で): **新規/変更した .ts/.tsx を含む impl 委譲の完了条件に `npm run build`(tsc) を必ず含める。かつセッションは裁定で `npm run build` と `npm run lint` を独立再実行し、impl の exit-code 報告を鵜呑みにしない**（vitest は型検査せず・tsc 増分キャッシュや楽観報告で「build 緑」が偽になり得る）。テストで `vi.fn()` の呼び出し引数を検証（`.mock.calls`）する場合、関数型注釈を変数に付けず `vi.fn<(a: A, b: B) => R>()` のジェネリック形で型付けする（注釈は Mock 型を消す・`_` 接頭辞の未使用引数は lint 非許容）
- 反映先: **CLAUDE.md 昇格済み**（「token 規律」/「委譲・レビューの規律」に「impl 完了条件へ tsc/build を含める＋セッション独立再実行」を追加。ST-18〔tsc 未課〕と ST-21〔報告不正確〕で同根 2 回目）
- 発生回数: 2 回目（ST-18 の「vitest 緑 ≠ tsc 緑」と合算。→ CLAUDE.md 昇格）
- 付記GOOD: ①複雑なダイアログ（PublishDialog）の render 検証を preview_eval（DOM 構造・i18n 解決・cover プレビュー・生キー漏れゼロ・submit disabled）＋screenshot（秘伝書意匠整合）で実施＝unit では見えない実描画を確認 ②Turnstile は localhost が許可ホスト外＝実投稿 e2e を代表できないため、環境依存検証（clipboard/Web Share/iOS と同じ相）としてユーザーのデプロイ環境へ引き渡し＋出口に明示 ③B1 の非コンポーネント export（loadTurnstileScript）を .ts 分離した react-refresh 対応は既存慣習（photoSourceContext/useExportActions）どおり機能

## 2026-07-08 scriptorium-s4-st22 state.md/lessons.mdを触る連続ループのPRは直前ブランチにスタックしてdocsを累積させる（同一挿入位置の衝突回避） [BAD→GOOD]

- 事象: S4を1マイルストーン=1ループ=1PRで進めた（ST-18マージ済/ST-19 #47/ST-20 #48/ST-22 #49）。各ループの出口でstate.md先頭とlessons.md先頭に新エントリを**同じ挿入位置**（`## 完了\n\n`直下）へprependする。ST-20ループでmainから新ブランチを切ったとき、state.mdがmainの版（ST-19エントリ不在・ST-18は「未コミット」表記のまま＝#47未マージのため）へ戻り、このまま進めるとPR #47と#48がstate.md/lessons.mdの同一行で3-way merge衝突する状態だった。ブランチをdelete-recipe(#47)基点へ切り直して回避し、以降ST-22も直前(cover-composer #48)にスタックさせてdocsを累積させた
- 原因: 独立した機能（scriptorium server / codex lib / scriptorium page）を並列PRにすると本来コンフリクトしないが、**全ループが共有する追記先ファイル（state.md・lessons.md）を各ブランチが先頭prependする**ため、mainから独立に切ると同一アンカーで必ず衝突する。「機能は独立」と「docsは共有単一ログ」のギャップ
- 一般化ルール (次ループの指示文としてそのまま使える形で): state.md/lessons.md を出口で更新する連続ループを未マージのまま積むときは、各ループのブランチを**直前ループのブランチ基点（スタック）**で切り、docsを累積させる（mainから独立に切ると先頭prependが3-way衝突する）。PRのbaseはmainのままでよいが、PR本文に「#N にスタック・#N マージ後に本PRは当該ループ分へ縮約・マージ順は番号順推奨」を明記する。ユーザーが番号順にマージすれば衝突ゼロ。未追跡の成果物ファイルはcheckoutを跨いでも保持されるので、ブランチ切り直しは成果物を失わずにできる（`git checkout <base>` → `git branch -D` → `git checkout -b`）
- 反映先: loop prompt（連続ループのPRスタック運用・docs累積）
- 発生回数: 1回目（BADの芽をセッションが出口前に自己検出・GOODに転じた）

## 2026-07-08 scriptorium-s4-st20 canvas系タスクは純関数＋依存注入でロジックをユニット化し実canvasはブラウザで焼込画素まで検証／低リスク純変換はopusレビューを裁定省略 [GOOD]

- 事象: ST-20（codex coverComposer＝crop焼込＋WebP品質二分探索）で、canvasがjsdom不動という制約に対し、lessons M2-dataの定石「①依存注入+spyでロジックをユニット ②実APIはブラウザ検証」を適用。`computeCropPixelRect`/`findWebpQualityBlob`を純関数化し、`composeCover`は`decode`/`encodeRegion`注入で二分探索の収束・dest寸法・ソース矩形をspy検算（canvas不要）。実canvasは**セッションがpreview_evalで`import('/src/lib/coverComposer.ts')`し、ノイズ実画像→composeCover→出力WebPを`createImageBitmap`で復号し、寸法(1600×1200)・サイズ(395KB=200-400KB帯命中)・thumb(55KB)に加え、左上cropのcover中心画素RGB[213,35,36]=赤優勢で「9引数drawImageの焼込が正しい」ことまで検証**。opusレビューは「純クライアント変換・セキュリティ/永続化面なし」を理由に裁定省略（一次ロジック解析＋網羅ユニット＋実canvas検証で担保）。implは今回もtsc型検査でunion型`naturalWidth`エラーを自己捕捉（tsc-gateルール3回目の実効）
- 原因（GOODの機序）: ①DOM/canvas依存は「純関数（座標・サイズ・探索ロジック）」と「DOM glue（canvas encode）」に分離すれば、前者はjsdomでユニット化でき後者はブラウザ検証に回せる ②画像生成物の検証は「type/サイズ/寸法」だけでなく「復号して画素をサンプルし変換（crop焼込・描画順）が意図どおりか」まで見て初めて中身を担保できる（share-card-v2の「生成物は中身まで見る」の canvas版） ③レビューのaltitudeは対象のリスク面（security/data-integrity/UI意匠）で決める＝純変換でそれらが無いタスクは、一次解析＋ユニット＋実機検証で代替してよい
- 一般化ルール (次ループの指示文としてそのまま使える形で): canvas/画像生成系のimplは「純関数（座標・探索・サイズ算出）＋DOM glue（encode/decode注入）」に分離させ、純関数はユニット・glueはブラウザ検証と役割を分ける（`composeCover`型の注入depsが定石）。生成画像のブラウザ検証は、出力を`createImageBitmap`等で復号し**寸法・サイズに加えて画素サンプルで変換の正しさ（crop焼込は既知色クアドラントの中心画素）を確認**する。レビューのaltitudeは対象のリスク面で決める＝security/永続化/データ整合/意匠仕様の突き合わせが無い純変換タスクは、セッションの一次ロジック解析＋網羅ユニット＋実機検証で担保しopusレビュー1ラウンドを省略してよい（省略の根拠を出口に明記する）
- 反映先: loop prompt（canvas系の純関数/glue分離とブラウザ画素検証・レビューaltitude裁定）
- 発生回数: 1回目（tsc-gateルールは3回目の実効＝下記ST-19エントリに合算。canvas純関数分離＝M2-dataの相の再適用・レビューaltitude裁定＝新）

## 2026-07-08 scriptorium-s4-st19 前ループのtsc完了条件ルールを適用し型リグレッションを実装内で自己捕捉／D1フェイクの正規表現ディスパッチは列名で新旧分岐を判別 [GOOD]

- 事象: ST-19（DELETE /api/recipes/:id）のimpl委譲で、前ループ（ST-18）のBAD学び「新規ファイル追加タスクの完了条件に`npm run build`(tsc)を含める」を適用した結果、implが作業中に自らTS6133（未使用paramのtsc失敗）を検出・修正し、型リグレッションが成果物に残らなかった（ST-18ではvitest素通り→build露見→追加委譲を要したのが、ST-19では委譲内で解決）。また、既存の`status='published'`固定の詳細SELECT分岐を壊さずに「全status取得の削除用フェッチ分岐」を追加する際、両SQLが`FROM recipes`+`WHERE id=?`にマッチする衝突を、削除フェッチ側のみが持つ列名`delete_pw_hash`を判別条件にし、かつ**詳細分岐より前に配置**することで解決。review R1(M1/L2/L3)→修正→**同一レビュアーへSendMessageでRound2依頼**（文脈保持・再Read不要）→PASS(C0/H0/M0/L0)も定石どおり機能
- 原因（GOODの機序）: ①完了条件に型検査を含めたことで、実装者の反復ループ内で型エラーが可視化され、セッションへ戻る前に自己修正された ②正規表現ディスパッチのフェイクは「新パターンが既存パターンの部分集合（同じFROM/WHEREにマッチ）」になりやすいが、新パターン固有の識別子（列名・SET句）を判別条件に足し、包含関係で限定的な方を上に置けば誤ディスパッチを防げる
- 一般化ルール (次ループの指示文としてそのまま使える形で): impl委譲の完了条件に`tsc`/`build`を含めるルールは有効＝継続適用する（同根の失敗が再発したらCLAUDE.md昇格）。正規表現ディスパッチのテストフェイクへ新SQL分岐を足すときは、(a)新SQL固有の識別子（列名・SET/RETURNING句）を判別条件に含める (b)既存分岐とSQLパターンが包含関係にある場合は、より限定的な新分岐を既存分岐の前に置く (c)判別条件の厳格化（例: `UPDATE recipes`→`UPDATE recipes`+`deleted_at=?`）は将来のSQL追加時の誤ディスパッチ＝偽緑を予防する。レビューのRound N+1は完了済みレビュアーへのSendMessageで文脈を保って依頼する（再Read不要で高速・低コスト）
- 反映先: loop prompt（フェイク拡張の定石・Round N+1のSendMessage運用）
- 発生回数: 1回目（ST-18のtsc-gate BAD学びが機能した実証。同根の失敗は再発せず）

## 2026-07-07 scriptorium-s4-st18 impl委譲の完了条件にtsc型検査を課さず・vitest緑がtsc緑を代表しない [BAD]

- 事象: ST-18を3波でimpl委譲した際、Wave1/2の完了条件を`npm run test`(vitest)/lint/prettierのみとし`tsc -b`(型検査)を課さなかった。vitestはisolatedModulesで型検査しないため、狭いlib/types構成の`tsconfig.server.json`（workers-types単独・DOM/node型なし）配下で型リグレッション（`globalThis.crypto`〔workers-typesは`crypto`直グローバルのみ宣言〕・`node:crypto`/`Buffer`〔@types/node無し〕・`FakeD1Database`→`D1Database`キャスト漏れ・`Record<string,unknown>`代入不可）が全て素通りした。さらにpostRecipe.tsが`@coat-codex/recipe-core`をimportしたことでバレル経由でDOM依存の`exportFile.ts`(`BlobPart`)がサーバーtsconfigに引き込まれる統合エラーも発生。これらはWave3で初めて`npm run build`を走らせたとき露見し、build緑化に追加1委譲を要した
- 原因: ①「テスト緑＝正しい」の暗黙前提。vitest(esbuild/isolatedModules)は型を検査しないため、tscでしか出ない型エラー（グローバル型解決・lib差・キャスト規約）を見逃す ②新規ソース/テストを追加するタスクの完了条件に`tsc`(または`npm run build`)を入れ忘れた ③パッケージをまたぐ新規import（recipe-core→server）が、消費側tsconfigのlib差で被import側パッケージ源を別libで再検査する統合面を、委譲設計時に想定しなかった
- 一般化ルール (次ループの指示文としてそのまま使える形で): **新規の.ts/.tsxソースまたはテストを追加するimpl委譲の完了条件には、必ず`tsc`型検査（`npm run build`または当該workspaceの`tsc -b`）をexit 0報告義務として含める**（`npm run test`(vitest)の緑はtscの緑を代表しない）。特にlib/typesが狭い専用tsconfig（Workers/workers-types・node専用等）配下へ新規ファイルを足すタスクでは、既存の見本ファイル（例: app.test.tsの`as unknown as D1Database`キャスト・atob方式base64・`crypto`グローバル直参照）の規約を委譲プロンプトに明示する。パッケージをまたぐimportを新設するタスクは、消費側tsconfigのlib（DOM有無）で被import側パッケージ源が型検査され得ることを想定し、DOM専用型（BlobPart等）の漏れを完了条件のbuildで検出する
- 反映先: loop prompt（impl委譲完了条件の標準セットに`tsc`/`build`を追加）・CLAUDE.md昇格候補（同種2回目で昇格）
- 発生回数: 1回目
- 付記GOOD: ①各ガードの発行SQLをD1フェイクのディスパッチと**対で**委譲プロンプトに明示した結果、reviewがINSERT列順=postRecipe/seed.mjs/§3.1/フェイクの4者一致を確認でき偽緑を排除できた（D1フェイク乖離リスクへの事前対処が機能）②review M1（rate増分がgarbageに枠を食わせる＝DoS懸念）を反射的に「修正」せず、仕様のST-28自動サーキットブレーカー設計（全Turnstile通過試行をglobal-postに数える＝攻撃検知シグナル・フィルタすると信号が消える）に照らして「現状維持が正」と裁定＝レビュー指摘を設計意図に接地して採否判断した好例（1次確認ルールの応用: 指摘の前提だけでなく「その修正が仕様の別機能を無効化しないか」まで確認）

## 2026-07-07 s3-deploy Build watch pathsに未定義記法`**`を計画書へ書き全コミットskipped／「自動ビルド正常」の判断が手動リトライ産の本番を証拠にしていた [BAD]

- 事象: ①技術計画§1.3に Build watch paths を `apps/codex/**` と書き（ST-03でユーザーがそのまま設定）、**Cloudflare Pagesのwatch pathsのワイルドカードは`*`のみ・かつ`/`を跨いでマッチが公式仕様**（`docs/*`が`docs/guides/setup.md`にマッチ）のため、`**`は未定義記法としてマッチ不全になり、**apps/codexを大改変したS1/S2マージまで含む全自動ビルドがskipped**になっていた（発見はユーザー「ビルドコマンド設定後から自動ビルドが全部スキップされてる」。108198f=S2マージのDetailsで Status: skipped を確定）。②その指摘の直前、セッションは「codex本番バンドル=現mainビルドとハッシュ一致」を根拠に「自動ビルドは正常」と誤裁定していた——その本番は**数分前のユーザー手動リトライが作ったもの**で、証拠が交絡イベントに汚染されていた
- 原因: ①ダッシュボード設定値の記法（glob方言）を公式docsで1次確認せず、一般的なglob慣習（`**`）を計画書に書いた——「セッション自身が計画書に書く事実にも1次確認ルールを適用」の適用漏れ（設定値の記法も「事実」）②「配信物が最新」という状態証拠から「自動プロセスが機能」という経路証拠を導いた——状態は複数経路（自動/手動）で到達し得るため、経路の証明には交絡イベントの時系列排除が必要だった
- 一般化ルール (次ループの指示文としてそのまま使える形で): 外部プラットフォームの設定値（glob記法・パスマッチ・cron式等）を計画書・手順書に書くときは、**そのプラットフォームの公式docsで記法仕様を1次確認**してから書く（一般的な慣習記法をそのまま使わない）。「自動プロセスXが機能している」と裁定するときは、状態証拠（成果物が最新）だけでなく**経路証拠**（Xのトリガー→実行記録、または交絡し得る手動操作が時系列上ないこと）を確認する。デプロイ系の裁定はダッシュボードの該当コミットのステータス確認を最初からユーザーへ依頼する（Skipped/Success はセッションから見えない）
- 反映先: 技術計画v1.3 §1.3是正（`apps/<name>/*`・`packages/*`・`package-lock.json`）・§9
- 発生回数: 1 回目（①は「1次確認ルール」ファミリーの新しい相=プラットフォーム設定記法。②は「検証が実利用を代表していない」の変種=証拠の時系列汚染）

## 2026-07-07 scriptorium-s3 Pages Functionsのcatch-allが静的アセットを遮蔽・curl完了条件の否定アサーション（grep -c 0）が404本文でも成立し素通り [BAD]

- 事象: ST-14の`functions/[[path]].ts`（Hono薄アダプタのcatch-all）が静的アセット配信とSPAフォールバックを遮蔽し、`/`・`/terms`等の**全SPAルートが404**になっていた（/api・/r/:idのみ生存）。ST-14の完了条件curlは「API応答」だけを検証し、ST-16の「`curl / | grep -c 'og:title'` → 0」は**404本文でも0が成立する否定アサーション**だったため、impl 2タスク＋レビュー4ラウンドを素通りした。セッション出口実機検証の最初のpreview_snapshot（=画面を見る）が即捕捉し、パススコープマウント（functions/api・functions/img・functions/r/[id]）へ是正・app.ts無改変（§4.1移行保険維持）で解消
- 原因: ①「Pages Functionsはマッチしたら応答が最終・アセットへ自動フォールバックしない」というプラットフォーム仕様を設計時に検算しなかった（§4.3の`functions/[[path]].ts`という計画記述をそのまま採用）②結合検証の完了条件が「作った機能（API/OGP）の正常系」に偏り、「壊し得る隣接機能（静的配信）」のルート網羅がなかった③否定アサーション（grep -c 0）は失敗モード（404・空応答）でも合格する
- 一般化ルール (次ループの指示文としてそのまま使える形で): API同居SPA（Pages Functions/ミドルウェア/プロキシ）の結合検証は、追加エンドポイントの応答に加えて**「非APIルートがHTML 200＋`id="root"`実在」を完了条件に必ず含める**。catch-allルーティングを追加する設計は「マッチしなかった要求がどこへ落ちるか」を計画段階で確定する（Pages Functionsのcatch-allは静的アセット遮蔽のため禁止=計画v1.2・CLAUDE.md反映済み）。完了条件に否定アサーション（grep -c 0・「含まれない」）を書くときは、同一応答への正例アサーション（ステータス200・期待要素の実在）と必ず対にする
- 反映先: CLAUDE.mdプロジェクト固有節（→同日v1.3で_worker.js規約に更新）・技術計画v1.2→v1.3 §4.3
- 発生回数: **2 回目 → CLAUDE.md 昇格済み**（2026-07-07 同日: PR #44マージ後の本番検証で**`functions/`ディレクトリ方式自体がRoot=`/`構成の本番Gitビルドで検出されない**ことが発覚〔バンドルハッシュ一致デプロイで/apiがSPAフォールバックHTMLを返却=パススコープ化の是正もローカルでしか機能していなかった〕。**ローカルpages devはwrangler.toml隣接のfunctions/を拾うが、本番はRoot directory相対でしか探さない**という環境差で、同一根本原因〔Pagesデプロイ構成仕様の未検算・ローカル結合検証が本番ビルドを代表しない〕の2回目。対処=`dist/_worker.js`（advanced mode）本採用〔出力ディレクトリ内のためRoot設定と無関係・非対象経路はenv.ASSETS.fetchへ明示フォールバック・§4.1移行保険と同型〕。「実機検証の規律」へ「デプロイ構成変更は本番/プレビューURLで正例アサーション付き確認まで完了としない」を昇格）
- 付記GOOD: ①S2出口で立てた「検証用分岐データはシードに事前設計」ルールが機能し、S2で実機不能だった**≠100警告バッジの実ブラウザ描画を今回初実証**（seed 5件のmix=[30,30]工程）②footerリンクのヒットテスト初回falseは「対象がビューポート外だとelementFromPointはnull」が原因=**ヒットテストはscrollIntoView後に行う**（掴み間違い・オーバーレイ遮蔽と並ぶ偽陰性の第3源）

## 2026-07-07 scriptorium-s2 出口実機の分岐データは事前シードが正——検証中のデータ操作では作れないことがある [BAD]

- 事象: S2出口実機検証で≠100警告バッジの実描画分岐を確認しようとしたが、preview実DBに警告ケース（mix合計≠100の工程）が存在せず、その場で作る3手段が全て不成立だった。①IndexedDB直接書き換え→権限分類器が「既存データの破壊」として拒否（妥当・データは無傷）②エディタUIへの合成inputイベント→MixRatioInput等が**draft state＋blur commit型**（onChange=draft更新のみ・commit()はblur/Enterでdraftを読む）のため、native setter＋Event('input')がReactのonChangeに届かずdraftが更新されず、blurしても旧値でcommit=無変更（入力のDOM value表示だけ変わり、次のprops再同期で復元される）③さらに最初の試行はaria-label="MIX B"の**document先頭一致が別工程の入力を掴んでいた**（before値75で発覚）。結果、警告分岐はunit（jsdom実レンダー）＋CSS逐語移動のbyte一致＋同一ファイル兄弟セレクタの実機カスケード実証で担保と裁定し、限界を出口レポートと申し送りに明記した
- 原因: ①実機検証の規律「分岐を変えるデータバリエーションを含める」を、検証時にデータを作れる前提で運用していた（シードに含める発想がなかった）②合成イベントで編集UIを駆動する前に、対象inputのcommit機構（直接onChange型かdraft+blur型か）をReadで確認しなかった③クエリを対象工程のコンテナにスコープせずdocument全域で先頭一致させた
- 一般化ルール (次ループの指示文としてそのまま使える形で): 出口実機検証が必要とする分岐データ（警告・エラー・境界値）は、**designの段階で検証用シードデータ（合成レシピ等）に含めておく**——検証中のDB直接書き換えは権限的にも原則的にも不可、編集UIの合成駆動は成立保証がない。合成イベントで編集UIを操作する検証は、対象inputのcommit機構をReadで確認してから設計する（draft+blur型はnative setter＋inputイベントでは駆動できない）。要素クエリは対象コンテナ（工程・カード等）にスコープし、操作前にbefore値で対象の同一性を検証する（掴み間違いはbefore値の矛盾で即発覚する）
- 反映先: loop prompt（実機検証の規律の運用詳細。M8-T45「実到達先確認」と同族の「対象同一性の事前検証」を含む）
- 発生回数: 1 回目

## 2026-07-06 scriptorium-plan 計画書レビューに「前提事実の実コード検算」観点を明示したらHigh 2件を実装前に検出した／範囲を定める記述はサンプル確認では足りない [GOOD]

- 事象: Coat Scriptorium技術計画v1のレビュー委譲で「計画が言及する既存コードの関数名・責務・構造を実ファイルで検算せよ」を観点②として明示した結果、(a)「SwatchChip/PartCard/PartReviewDialogを注入化」という記述が実際は`resolvePhotoUrl`直依存12+部品の取りこぼしで「移動対象」と「注入要否」が混線していたこと (b)coverComposerが再利用するとした`encodeFromSource`がquality 0.9固定で品質二分探索に使えないこと、を**実装着手前**に検出できた。セッション自身も計画前提4点（関数実在・注入済み・直import・スキーマ版数）をGrepで1次確認していたが、SwatchChip 1点のサンプル確認から「注入化対象はこの3部品」と**範囲を一般化した記述**を書いたのが穴だった
- 原因: 既存の1次確認ルール（CLAUDE.md昇格済み）は「言及する事実が存在するか」の点検証には効くが、計画書の「〜のみ」「〜等」のように**範囲を定める記述**は依存の全量列挙（Grep横断）をしないと正しさを保証できない。m7「再利用指定が要求仕様と矛盾」と同族の欠陥を、今回はレビュー観点の明示が事前に捕捉した（規律が機能した成功例）
- 一般化ルール (次ループの指示文としてそのまま使える形で): 新規サブシステムの計画書・設計書のレビュー委譲には「計画が前提とする既存コードの事実（関数名・シグネチャ・依存の広がり）を実ファイルで検算」を必ず観点に含める。計画書に範囲を定める記述（「Xのみ注入化」「Y等を移動」）を書くときは、サンプル1点の確認でなく該当パターンのGrep横断で全量を列挙してから範囲を確定する。プラットフォーム依存の断定（「AはBでは動かない」）は2案併記＋実機確定タスク化で計画に固定しない
- 反映先: loop prompt（m7-share-feedback「再利用部品の能力確認」・CLAUDE.md 1次確認ルールの適用範囲を「範囲を定める記述の全量列挙」へ拡張する運用）
- 発生回数: 1 回目（同根のm7「既存部品再利用指定の矛盾」〔2026-07-03〕は事後検出・今回は事前検出。欠陥の相は同族だが規律側が機能した）

## 2026-07-06 sns-hashtag 数値アサーション再計算の裁定は「値の緑」でなく「導出の各項」を実装Readで検算する [GOOD]

- 事象: ハッシュタグ変更（タグ長11→10字）に伴うimplのトリム境界テスト再計算は、アサーション値（本文134字・count280）が正しくテストも緑だったが、コメントの導出が実装と不一致だった（「tagWeight(" #coatcodex")=11…267→133.5→134文字」と切り上げで辻褄合わせ。実際はtrimWithFixedTagのタグ再付与がスペースなし=tagWeight10・「…」U+2026はweight1レンジ外=weight2・本文予算268=134字ちょうど）。セッション裁定が「切り上げはこの機序ではあり得ない」という違和感から実装をReadして検算し、コメントのみglue修正。旧コメント（変更前）も同種の導出誤りを含んでおり（値は偶然一致）、implはそれを踏襲していた
- 原因: テストの緑は「値」しか保証せず、「導出コメント」の正しさは検証しない。誤った導出は次の仕様変更時（今回がまさにそれ）に再計算の出発点として誤誘導する。既存コメントの踏襲は誤りの伝搬経路になる
- 一般化ルール (次ループの指示文としてそのまま使える形で): 境界値テストの数値再計算を裁定するときは、アサーション値の緑だけで採用せず、導出コメントの各項（重み・区切り文字の有無・丸め方向）を実装Readで検算する。「切り上げ」等、機序として不自然な丸めが導出に現れたら誤導出のシグナルとして実装を確認する。誤った導出コメントは値が正しくてもその場で直す（次の変更の出発点になるため）
- 反映先: loop prompt（「レビュー指摘・タスク前提の事実の1次確認」昇格ルールの適用対象に「テストコメントの導出過程」を含める運用）
- 発生回数: 1 回目

## 2026-07-05 partreview-onclose worktree運用でselfcheckのgitコマンドが本体リポジトリ側で実行され「差分なし・コミット済み」の事実誤認報告 [BAD]

- 事象: worktree（.claude/worktrees/stoic-wing-64b1df）での小修正ループで、selfcheckが「git status clean・差分なし・成果物はコミット済み（HEAD=ae51780）」と報告した。しかしae51780はworktreeに存在しない本体リポジトリ側のコミットで、selfcheckのgitコマンドは委譲プロンプトに作業ディレクトリを絶対パスで明記していたにもかかわらず本体リポジトリ側で実行されていた。セッションが`git -C <worktree絶対パス>`で1次確認し、worktreeには期待どおりの未コミット差分（2ファイル・+13/-1）が実在することを直接確認して裁定した。lint/prettier/禁止パターンGrepの結果も本体側の可能性があるが、implが同ゲートをworktree内でexit 0報告済みだったため二重化で吸収された
- 原因: subagentのシェルは委譲プロンプトの文言でなく自身の起動時cwdや`cd`の成否に依存する。git worktreeは本体と履歴を共有するためコマンドが「成功して違う答えを返す」（エラーにならず誤ったHEAD・cleanなstatusが返る）分、通常のパス間違いより検出しにくい
- 一般化ルール (次ループの指示文としてそのまま使える形で): worktree運用でsubagentへgit確認を委譲するときは、コマンド列を`git -C <worktree絶対パス> status`の形で`-C`付きで指定し、確認項目に「HEADが期待コミット（委譲時に明記）と一致するか」を含める。subagentの報告が「差分なし・クリーン」等の否定結果で設計前提（未コミット差分があるはず）と矛盾したら、修正着手前にセッションが同じコマンドを直接実行して裁定する
- 反映先: loop prompt（selfcheck/review委譲プロンプトのgitコマンド指定形式）
- 発生回数: 1 回目

## 2026-07-04 print-preview transform-origin:centerの中央配置前提がoverflow時のmargin:auto=0で崩れた（右端切れ） [BAD]

- 事象: FB-Dの印刷プレビューモバイル縮小（`--print-scale`注入）で、`.scaleInner`の`transform-origin: top center`が「紙面は中央配置済み」を前提にしていたが、縮小が必要な幅では`.sheet`(794px固定)が親より広く`margin:auto`が0に解決されて左寄せになり、中央origin縮小で紙面が右へずれ右端が切れていた（620pxで68px）。FB-D出口検証は「縮小が適用されたこと」を確認したが「縮小後の紙面が全幅に収まること（右端の座標）」を確認せず、ユーザーiPhone実機（T43③確認中）で発覚
- 原因: ①CSSの暗黙前提（margin:autoによる中央配置）は子が親より広いと成立しない（auto→0解決）ことを設計時に検算しなかった ②検証が「機構が動いた」で止まり「結果の幾何（収まり）」に達していなかった（share-card-v2「実体確認の深さ不足」と同じ相）
- 一般化ルール (次ループの指示文としてそのまま使える形で): transform: scale系の縮小表示を検証するときは、倍率の適用確認で止めず、**縮小後のboundingClientRectが利用可能幅に収まること（left≥0かつright≤viewport幅）**を全対象幅で検算する。transform-originにcenterを使うときは「原点基準の中央配置が縮小対象の幅超過時にも成立するか」（margin:autoは子が親より広いと0になる）を確認し、成立しないならleft起点＋数式で収める
- 反映先: loop prompt（「検証の深さ不足」系の再発。既存昇格ルール「生成物は中身まで見る」の幾何版）
- 発生回数: 1 回目（同根の「実体確認の深さ不足」〔share-card-v2〕と合算で2回目 — 次回同種が出たらCLAUDE.md実機検証の規律へ「縮小/配置系は結果の幾何を検算」を昇格）

## 2026-07-04 mobile-fullpage T44ネストルート化が「モバイル＝フルページ」の暗黙前提を無効化（実機出荷後にユーザー検出） [BAD]

- 事象: T44で`/recipe/:id`を親子同時マウントのネストルートへ変更した際、「子ページが親を置き換える」というルーティングの暗黙前提が消え、モバイル幅ではPartEditorPageが通常フローでOverviewの下（画面外）に描画されるだけになった。Overview側のinert付与（幅無条件）と重なり「可視画面が全タップ不能」のリグレッションとなり、M8出口検証・レビュー2ラウンドを素通りしてユーザースマホ実機で発覚（「パーツを追加でフローを経ず追加され操作不能」）。M8出口の「モバイル375pxフルページ」検証は遷移の成立を見ただけで、**遷移後に視認している画面がフルページの編集UIか**（viewport内elementFromPoint）を確認していなかった
- 追加の学び: 修正時、`display:none`のDOMコミットで文書高が縮むとscrollYは同コミットで0へクランプされるため、effect内でのスクロール位置退避は常に0になる（退避はDOM変更前=navigateを呼ぶハンドラ内で行う）。これも静的レビューでは検出されずセッション実機検証（開前819.5→閉後0）が捕捉した
- 一般化ルール (次ループの指示文としてそのまま使える形で): ルート構造の変更（ネスト化・親子同時マウント化・レンダー先変更）は「置き換え表示」等の暗黙前提を無効化するため、出口実機検証は**全ブレークポイント幅×主要フロー**で「遷移後に視認している画面」へのヒットテストを行う（URL遷移の成立だけで合格にしない）。display:none等で文書高を変えるUIのスクロール退避はDOM変更前のイベントハンドラで読む
- 反映先: **CLAUDE.md 昇格済み**（「委譲・レビューの規律」のDOM位置変更リストへ「ネストルート化等のマウント構造変更」を追加＋「実機検証の規律」へ全ブレークポイント幅の視認検証を追加。M6 z序列逆転と同一根本原因〔DOM/マウント位置変更が暗黙の前提を無効化〕の3例目・「検証が実利用を代表していない」系とも同根）
- 発生回数: 1 回目（同根のM6 z序列・M3レビュー修正リグレッションと合算で3回目）

## 2026-07-03 M5-export 実機検証の代表性不足（写真0枚のみで往復確認） [BAD]

- 事象: 「export→import一巡の実機確認済み」としたが検証レシピが写真0枚だったため、写真ありで必ず失敗するDexie tx罠（tx内で`fetch(dataUrl)`をawait→txが自動失効→"Transaction has already completed or failed"）を見逃し、ユーザー実機（写真2枚・スマホ→PC往復）で発覚
- 原因: ①実機検証データが機能の主要分岐（写真あり/なし）を代表していなかった ②レビューR1が同箇所のPromise.allを「メモリ観点」で指摘済み・R2でtx罠パターンをduplicateRecipeへ適用していながら、同一パターンの横断適用（importRecipe本体）をしなかった ③ユニットテストは`dataUrlToBlob`を即時resolveスタブで注入していたため罠が現れなかった
- 一般化ルール (次ループの指示文としてそのまま使える形で): 実機検証シナリオには機能の分岐を変えるデータバリエーション（写真あり/なし・0件/複数件）を必ず含める。レビューで危険パターンを1箇所指摘したら同一パターンをGrepで横断適用する。依存注入でスタブする外部APIには「実装と同じ非同期タイミング（マクロタスク遅延）」のスタブを最低1ケース入れる
- 反映先: **CLAUDE.md 昇格済み**（「実機検証の規律」— 検証の忠実度不足entryと同一根本原因〔検証が実利用を代表していない〕の2回目）
- 発生回数: 1 回目（同根の「検証の忠実度不足」と合算で2回目）

## 2026-07-03 M5-export レビューと実機検証の相互補完 [GOOD]

- 事象: UI層レビュー（R1）が見逃した「Home通常状態のImportJsonButton欠落」を、セッション実機検証のデザイン決定稿突き合わせが検出した。逆にレビューは実機で気づけないロジック欠陥（fetch失敗時の一括降格・Dexie tx罠）を検出し、2経路が補完し合った
- 原因: diffベースの静的レビューは「差分に現れない欠落（存在すべきなのに無いもの）」の検出が構造的に苦手。実機検証は画面単位で決定稿の要素実在を見るため欠落に強い
- 一般化ルール (次ループの指示文としてそのまま使える形で): UI層の出口では、レビュー委譲と並行してセッションが「決定稿にあるべきUI要素の実在確認」を実機で行う。レビュー委譲プロンプトにも「差分外も含め決定稿との突き合わせ再点検」を1観点として明示する
- 反映先: loop prompt
- 発生回数: 1 回目

## 2026-07-03 M5-export preview環境のCSSアニメーション停止 [BAD] → CLAUDE.md 昇格済み

- 事象: ボトムシートが「開かない」ように見えた（translateY(100%)のまま座標検証が全滅）。実バグではなく、previewタブが `document.hidden=true` のときブラウザがCSSアニメーションのタイムラインを進めない環境制約だった（`getAnimations()`で `playState: running / currentTime: 0` を確認）
- 原因: 非表示タブではレンダリングパイプラインが止まり、`animation:`起点の開閉UIは初期キーフレーム位置に留まる。fill-mode未指定だと「閉じ位置で固まって見える」
- 一般化ルール (次ループの指示文としてそのまま使える形で): previewでアニメーション付きオーバーレイUIの座標検証を行う前に `el.getAnimations().forEach(a => a.finish())` で最終状態へ送る。「開かない・位置がおかしい」ように見えたら、コードを疑う前に `document.hidden` と `getAnimations()` の currentTime を確認する
- 反映先: loop prompt → **CLAUDE.md 昇格済み**（実機検証の規律へviewport相を追記 2026-07-08）
- 発生回数: 2 回目（2026-07-08 S5出口: **背景タブがviewport 0×0のままレイアウトされず`elementFromPoint`が全対象null**＝ヒットテスト偽陰性。preview_screenshotは正常描画＝「スクショは生きているがevalの座標系が死んでいる」乖離が起きる。preview_resize＋再evalで回復。同根「非表示タブのレンダリング停止」のviewport相 → 昇格）

## 2026-07-03 mobile-ux-feedback 検証の忠実度不足 [BAD]

- 事象: ボトムシートのpointer-events透過バグ（✕・ハンドルが全て背後に素通し）を、実機確認済みとして出荷してしまった。検証がJSの`el.click()`直呼びだったためヒットテストを素通りし、後にユーザー実機で発覚
- 原因: `el.click()`はブラウザのヒットテスト（どの要素がタップを受けるか）を経由しない。overlay系UIはハンドラが正しくても座標上で受け取れないバグを持ち得る
- 一般化ルール (次ループの指示文としてそのまま使える形で): 操作系UIの実機検証では`document.elementFromPoint(中心座標)===対象要素`のヒットテストを必ず併用する。preview_clickはDPR≠1の環境で座標がズレて`<html>`に着弾することがあるため、失敗時はelementFromPoint＋座標付きイベントdispatchで代替する
- 反映先: loop prompt → **CLAUDE.md 昇格済み**（「実機検証の規律」）
- 発生回数: 2 回目（2026-07-03 M5でも同根〔検証が実利用を代表していない〕のデータ代表性不足が発生 → 昇格）

## 2026-07-03 mobile-ux-feedback 並行委譲中のgit操作 [BAD]

- 事象: implエージェントが並行作業中に`git stash`を実行（直後にpopで復元・実害なし）。別エージェントの未コミット成果物を巻き込み消失させるリスクがあった。別のエージェントは禁止明記後に`git status`を1回実行し自己申告
- 原因: 委譲プロンプトの「git操作は不要」という表現が禁止として弱く、エージェントの自律判断でgitコマンドが混入した
- 一般化ルール (次ループの指示文としてそのまま使える形で): 並行委譲時のimplプロンプトには「git操作は禁止（stash/checkout/reset等の状態変更は絶対に行わない）」と禁止形で明記する。並行でない場合も成果物の管理はセッションの責務としgitはセッションのみが触る
- 反映先: loop prompt / CLAUDE.md 昇格候補
- 発生回数: 1 回目

## 2026-07-03 official-paint-charts [GOOD]

- 事象: ユーザー提供のカラーチャート（Citadel/Vallejo）を、Coat d'armsで確立した「docs/paints/*-master.json（原本）→ public/paints/*.json（配信・再生成）」の2層構成にそのまま載せられ、AK除外やレンジ付与も配信層の再生成だけで完結した
- 原因: 原本と配信を分離し、配信は常に原本から機械生成する構造にしていたため、データ差し替えが安全な単方向処理になった
- 一般化ルール (次ループの指示文としてそのまま使える形で): 外部由来データは「原本（docs・出典明記・無加工に近い形）」と「配信（アプリ最適形・原本から再生成）」の2層で持ち、配信側を直接手編集しない
- 反映先: loop prompt
- 発生回数: 1 回目

## 2026-07-02 M0-foundation [GOOD]

- 事象: T1〜T6の全impl委譲が一発で全ゲート（install/test/lint/build/prettier）通過し、出口一括opusレビューもPASS（C0/H0）
- 原因: 計画v2.2のタスク粒度（成果物ファイル・依存・完了条件が明記済み）と、impl委譲プロンプトに「完了条件コマンド列＋exit code報告」を義務付けた運用
- 一般化ルール (次ループの指示文としてそのまま使える形で): impl委譲プロンプトには必ず「仕様の正の§番号」「完了条件のコマンド列とexit code報告義務」「スコープ外ファイルの明示」を含める
- 反映先: loop prompt
- 発生回数: 1 回目

## 2026-07-02 M1-logic [GOOD]

- 事象: 独立タスク（T10/T11）の並列委譲が衝突なく機能し、レビューに「テストの実質性・境界値」観点を明示指定したところ境界値の独立再計算による検証まで実施された
- 原因: 並列委譲時に「相手エージェントの成果物ファイルへの不可侵」をプロンプトで明示／レビュー観点をマイルストーンの性質（ロジック層）に合わせて具体化した
- 一般化ルール (次ループの指示文としてそのまま使える形で): 成果物ファイルが重ならないタスクは並列委譲してよいが、互いのファイル名を明示して不可侵を指示する。レビュー委譲時は観点をマイルストーンの性質に合わせて具体化する（ロジック層=境界値と写経テスト検出、UI層=デザイン仕様との突き合わせ）
- 反映先: loop prompt → **CLAUDE.md 昇格済み**（「委譲・レビューの規律」）
- 発生回数: 2 回目（2026-07-02 M2-dataでも同運用が機能: T12/T13・T14/T15の2回の並列委譲とデータ層観点レビュー）

## 2026-07-02 M2-data [GOOD]

- 事象: fake-indexeddb+jsdomの制約（structured cloneでBlobが化ける・canvas不動作）に対し、テストはspy/依存注入で配線検証に徹し、実挙動はプレビューサーバー上のcanvas実機確認（4経路）で補完する二段構えが機能した
- 原因: テスト環境で再現不能な領域を「検証しない」のではなく、検証手段を層別に分けた
- 一般化ルール (次ループの指示文としてそのまま使える形で): テスト環境で再現できないブラウザAPI依存部は、①依存注入+spyでロジック分岐をユニットテスト ②プレビューのpreview_evalで実APIの経路を実機確認、の2層で必ず両方カバーする
- 反映先: loop prompt
- 発生回数: 1 回目

## 2026-07-02 M3-presets 実機スパイク検証 [GOOD]

- 事象: ユニットテスト280件が全緑でも見つからなかった「state正・UI誤」の乖離バグ（PaintPickerのvalue復元不全）を、自己マウント式スパイク（src/dev/M3Spike.tsx＋preview_eval）の実機検証が発見した
- 原因: RTLテストは単発レンダー中心で、再レンダー・実fetch・実フォーカスを跨ぐUI状態の持続性を検証しにくい
- 一般化ルール (次ループの指示文としてそのまま使える形で): UI部品のマイルストーンは出口で必ず自己マウント式スパイクによる実機検証を行う（新スパイクはアプリのモジュールグラフ内から自己マウントさせる — evalから/@id/でreactをimportすると二重インスタンスで壊れる）
- 反映先: loop prompt
- 発生回数: 1 回目

## 2026-07-02 M3-presets レビュー修正がリグレッションを生成 [BAD]

- 事象: Round 1指摘への個別修正（key={colorId}化・palette再利用条件）が相互作用し、新規High 2件（チップ写真サイレント損失・ピッカー対話破壊）を生んだ。Round 2再レビューで捕捉
- 原因: 個別には妥当な最小修正を同一バッチで適用し、修正同士の複合作用の検証をimplのテストゲートだけに頼った
- 一般化ルール (次ループの指示文としてそのまま使える形で): レビューFAILの修正バッチ後は必ず同一レビュアーでRound N+1判定を取り、「修正同士の複合作用・修正が導入した新規問題」を明示観点に含める。UI状態管理（key・effect・確定タイミング）に触れる修正は特に要注意
- 反映先: loop prompt → **CLAUDE.md 昇格済み**（「委譲・レビューの規律」— 2026-07-03 M6のz序列逆転と同一根本原因〔レビュー修正が新規問題を導入〕の2回目）
- 発生回数: 2 回目（2026-07-03 M6-print-snsでリフトアップ修正がz-index序列逆転を導入 → 昇格）

## 2026-07-02 git初期化事故（セッション操作） [BAD]

- 事象: フォルダに既存の.git（ユーザーの手元コミット）があるのに新規初期化前提でgit操作し、checkoutで作業ツリーからファイルが一時的に消えた（既存コミットが全ファイルを保持していたため完全復旧、損失ゼロ）
- 原因: セッション開始時の環境情報「git repoではない」を、時間経過後（ユーザーが手元でinit/commit済み）も再検証せず使った
- 一般化ルール (次ループの指示文としてそのまま使える形で): 状態を変えるgit操作（init / checkout / branch -D / reset）の直前に必ず `git status && git log --oneline -3` で現状を確認する。セッション開始時のスナップショット情報は陳腐化する前提で扱う
- 反映先: CLAUDE.md 昇格候補
- 発生回数: 1 回目

## 2026-07-02 M4-editor impl委譲の外部要因切断（月次利用上限） [BAD]

- 事象: T25のimpl（Sonnet）が月次利用上限で切断され報告なしに停止（成果物はほぼ完成・テストのbeforeEach書き忘れと整形のみ欠落）。T27の初回委譲も着手前に切断（成果物ゼロ）
- 原因: 委譲先の失敗はプロンプト起因とは限らない。外部要因（利用上限・接続断）では「無報告のまま部分成果物が残る」状態になる
- 一般化ルール (次ループの指示文としてそのまま使える形で): impl委譲が報告なしに終了したら、まず `git status`＋全ゲート実行で残骸の完成度を判定する。①ほぼ完成→欠落のみセッションglue補完（切断点の痕跡=未使用importなどから欠落を特定）②成果物ゼロ→同一プロンプトで再委譲。中途半端な残骸の上への再委譲はしない
- 反映先: loop prompt → **CLAUDE.md 昇格済み**（「委譲・レビューの規律」に追記）
- 発生回数: 3 回目（2026-07-02 M4で2件／2026-07-03 公式チャート差し替えタスクで1件=npm実行中切断・成果物完全のためコミットのみで回収／2026-07-07 S0 ST-01=600s無進捗ストール・残骸判定「ほぼ完成」→セッションglue回収で昇格済みルールがそのまま機能）

## 2026-07-02 M4-editor マイルストーン境界のタスク番号の谷間 [GOOD]

- 事象: M4入口の依存検算でT16（useRecipeStore）がM2完了(T12〜T15)とM3完了(T17〜T21)の番号の谷間で未実装のまま残っていたことを発見し、M4先頭で実装してブロッカーを回避した
- 原因: マイルストーンをタスク番号レンジで運用すると、レンジ境界のタスクがどちらの完了条件にも含まれず脱落し得る
- 一般化ルール (次ループの指示文としてそのまま使える形で): マイルストーン入口で「前マイルストーンまでの完了タスク番号が連続しているか」を検算し、谷間のタスクは着手前に依存グラフへ組み込む
- 反映先: loop prompt
- 発生回数: 1 回目

## 2026-07-03 M6-print-sns リフトアップ修正がz-index序列を逆転（実機ヒットテストで捕捉） [BAD]

- 事象: T40レビューM1（fixed-in-transform）対応のリフトアップ修正が、ShareDialogをExportSheetのスタッキング文脈（z:200）の外へ出したことでz序列を逆転させ（sheetBackdrop 200 > ShareDialog backdrop 60）、モバイルで共有ダイアログの全タップがシートbackdropに吸われるリグレッションを生成。同一レビュアーのRound 2はpointer-events継承・transform残存まで検証したが、z序列の再検証はセッションの観点指定からも漏れた。出口実機検証の`document.elementFromPoint`ヒットテスト（既存規律）が捕捉し、backdrop z-index 60→300の1行glueで解消・全ヒットテスト通過を再確認
- 原因: DOM位置を変える修正（リフトアップ/portal化/レンダー先変更）は「元の位置で成立していた暗黙の前提」（スタッキング文脈内での重なり順）を無効化する。jsdomはz-indexの実効を検証できず、DOM順アサーション（compareDocumentPosition/contains）では実際の重なりを保証できない
- 一般化ルール (次ループの指示文としてそのまま使える形で): オーバーレイUIのDOM位置・レンダー先を変える修正では、「修正が無効化した暗黙の前提」（z-index序列・スタッキング文脈・イベント伝播経路）をRound N+1の明示観点に含め、修正後に実機ヒットテストで全インタラクティブ要素を再検証する
- 反映先: **CLAUDE.md 昇格済み**（「委譲・レビューの規律」— M3「レビュー修正がリグレッションを生成」と同一根本原因の2回目）
- 発生回数: 1 回目（同根のM3-presetsと合算で2回目）

## 2026-07-03 base-externalize 合成データで既存部品を再利用したUI再構成 [GOOD]

- 事象: BASE表示のオーバーレイ→カード化で、BaseStepCard等の新部品を複製実装せず、合成part `{id:"base", name, steps: baseSteps}` を既存PartCardへ渡す方式を採用。PartCardの変更は「order省略時に番号セル非描画」の1点のみで、サムネ規約・STEPタグ・混合バッジ・工程レビューの全機能とビジュアル一貫性が自動継承された。スキーマ側に最初から予約語 "base"（INV-17）と予約ルート /part/base が確保されていたため、センチネルの衝突検討も不要だった
- 原因: UI再構成の設計時に「新コンポーネント複製」より先に「既存部品へ合成データを渡せないか」を検討した。予約語・予約ルートをスキーマとルーティングの両方で最初に確保していた設計が拡張の受け皿として機能した
- 一般化ルール (次ループの指示文としてそのまま使える形で): UI再構成のdesignでは、新コンポーネント作成の前に「既存部品が受け取れる形にデータを合成できないか」を必ず検討する（変更点が最小になり意匠の一貫性が自動で保たれる）。センチネル値はスキーマ（不変条件）とルーティングの両方で予約してから使う
- 反映先: loop prompt
- 発生回数: 1 回目

## 2026-07-03 share-card-v2 実機検証の「実体確認の深さ」不足（DL寸法のみで実ピクセル未確認） [BAD]

- 事象: M6出口の実機検証で共有カードのDLファイルを「寸法1200×900・image/png・連番名」まで確認して合格としたが、実ピクセルを1度も表示して見なかったため、「描画順序バグで写真が背景色に全面上書きされ全カードが無地」という致命的欠陥を見逃し、ユーザーのスマホ実機で発覚した。修正後の検証では「DL blobをdataURL化して画面に表示→スクリーンショット」の手順を確立し、以後のカード変更（v2）は全て実ピクセルで確認した
- 原因: 検証が「成果物のメタデータ（寸法・型・名前）」で止まり「成果物の中身（描画内容）」に達していなかった。ユニットテストも描画呼び出しの配線のみで順序（後の全面fillRectが写真を消す）を固定していなかった
- 一般化ルール (次ループの指示文としてそのまま使える形で): 生成物（画像・PDF・ファイル）の実機検証は、メタデータ確認で止めず**中身をレンダリングして目視**する（画像はdataURL化して画面表示＋スクリーンショット）。canvas等の重ね描画はテストで「描画順序」を固定する（呼ばれたかではなく、何が何の後に描かれるか）
- 反映先: loop prompt / CLAUDE.md 昇格候補（「検証が実利用を代表していない」系の3例目だが、新しい相=検証の深さ不足。既存昇格ルールに「生成物は中身まで見る」を加える価値があれば次回昇格）
- 発生回数: 1 回目

## 2026-07-03 m7-share-feedback 「既存部品を再利用せよ」指定が要求仕様と矛盾する指示になった [BAD]

- 事象: FB-2委譲プロンプトで「使用カラーは色名・ブランド併記のグリッド。**既存のdrawSwatchGrid系描画を再利用**すること」と指示したが、drawSwatchGridが色名を一切描かない関数であることをdesign時に確認していなかった。implは矛盾する2指示のうち「再利用」を優先して名前なしスウォッチのまま出荷し、レビューRound 1/2も「drawSwatchGrid再利用の整合」を観点にしたため乖離を検出できず、実機実ピクセル目視まで問題が到達した（追加1委譲＋1レビューラウンドを消費。ユーザーも同一問題を指摘）
- 原因: 再利用対象の既存部品の「実際の能力」（何を描き何を描かないか）を確認せず、部品名の印象（Grid=リッチな一覧）で要求仕様を満たすと思い込んだ。矛盾した委譲プロンプトはimplの解釈次第でどちらにも倒れる
- 一般化ルール (次ループの指示文としてそのまま使える形で): 委譲プロンプトで既存部品の再利用を指定するときは、その部品が要求仕様の表示要素を実際に満たすことをdesign時にRead/Grepで確認してから書く。確認していない場合は「再利用できるか確認し、できなければ新設」と明示的に判断を委ねる形にする（「〜系を再利用」という曖昧指定は矛盾指示の温床）
- 反映先: loop prompt
- 発生回数: 1 回目

## 2026-07-03 m7-share-feedback 実ピクセル目視ルールがレビューPASS済みのdesign乖離を捕捉 [GOOD]

- 事象: FB-2はレビュー2ラウンドPASS（静的検算で算術・境界値・型整合まで健全）だったが、出口実機の「DL blobをdataURL化して画面表示→スクリーンショット」目視が、designとの乖離2点（使用カラーに色名なし・8行固定予約による中央240px空白）を即座に検出した。ユーザーが同じ指摘をする前にセッション検証で捕捉・修正着手できていた
- 原因: 静的レビューは「実装がimplの主張どおりか」を検証するが、「描画結果がdesign意図・ユーザー期待を満たすか」は実物のピクセルでしか判定できない。share-card-v2の学び（生成物は中身まで見る）をルール通り適用したことが機能した
- 一般化ルール (次ループの指示文としてそのまま使える形で): canvas/画像生成系のタスクは、レビューPASSでも「実データでの実ピクセル目視」を出口の必須ゲートとして扱う（レビューと実機は検出できる欠陥のクラスが異なる補完関係。どちらか一方の通過を完了条件にしない）
- 反映先: loop prompt（share-card-v2 entryのルールが機能した実証。「実機検証の規律」の運用継続）
- 発生回数: 1 回目

## 2026-07-03 brand-range 実行中implへのSendMessage追加指示が注入攻撃と誤認され不実施 [BAD]

- 事象: 実行中のimplへSendMessageで送った正規の追加指示（色名130px上限撤廃）を、エージェントが「ツール結果に埋め込まれた不審な注入指示」と判断して従わず、当初の受託タスクのみを完了した。完了報告の精査（「従いませんでした」の明記）で不実施を検出し、指示全文を本文に含む新規タスクとして再委譲して解決。損失は1委譲分の時間のみ
- 原因: SendMessageの転送はツール結果内のテキストとして届くため、プロンプトインジェクションを警戒するエージェントには正規指示と区別がつかない。「委譲プロンプト本文が唯一の正」という受託側の健全な防御と、「実行中に仕様を足したい」セッション側の運用が構造的に衝突する
- 一般化ルール (次ループの指示文としてそのまま使える形で): 実行中のimplへ仕様の追加・変更をSendMessageで送らない。①完了を待って新規タスクとして委譲する（指示全文を本文に含める）か、②次回から最初の委譲プロンプトに含める。SendMessageの使用は「完了済みエージェントへの報告再送依頼」「同一レビュアーへのRound N+1依頼」等、既存文脈の継続に限る。implの完了報告に「指示に従わなかった」旨の記述がないか毎回精査する
- 反映先: loop prompt
- 発生回数: 1 回目

## 2026-07-03 M7後半 不可視ヒット領域拡張のoverflowクリップ [BAD]

- 事象: モバイルタッチ44px化で、小型ボタン（StepPhotoTile✕ 24px）の`::before{inset:-10px}`外向き拡張が、祖先の`.thumb{overflow:hidden}`にクリップされ上・右方向のヒット領域が実際には拡がっていなかった。全ゲート緑・レビューPASSでもすり抜け、セッションの実機ヒットテスト（elementFromPointを4方向）が検出
- 原因: 「::before insetでヒット領域を広げる」定石が、要素の配置（overflow:hiddenの子孫か・コーナー絶対配置か）に依存することを考慮していなかった。ユニットテストもレビューもDOM幾何を評価しない
- 一般化ルール (次ループの指示文としてそのまま使える形で): 不可視ヒット領域拡張（::before負inset）を使うときは、①対象が`overflow:hidden`祖先の内側にあるか、②コーナー絶対配置で外向きに拡張しているかを確認し、クリップされる方向へは拡げない（内向き＝クリップされない方向へ寄せる）。実機検証は中心1点でなく上下左右4方向のelementFromPointで各要素44px到達を確認する
- 反映先: loop prompt（実機検証の規律に追加候補）
- 発生回数: 1 回目

## 2026-07-04 M8後半T45 実機検証の「否定結果」を早計確定しかけた（非同期反映・オーバーレイ誤着弾） [BAD]

- 事象: T45実機検証で3段の誤判定リスクに連続遭遇。①preview_clickがDPRズレで対象ボタンでなくページコンテナDIVへ着弾し「クリックが吸われた（バグ未解消）」ように見えた ②合成イベント列でボタン到達＋ハンドラ発火まで計装確認したのに、直後の同期計測でスロット数が増えておらず「追加が効かない」と誤判定しかけた（Reactの状態反映が非同期で、1マクロタスク後の再計測では正しく2になっていた）③色名編集中はサジェストドロップダウンがボタンを覆う局面があり、事前ヒットテスト時点と実クリック時点で最前面要素が変わった
- 原因: 「機能していない」という否定結果は、(a)イベントが対象に到達していない (b)反映がまだ (c)本当のバグ、の3通りの原因を持つが、検証手順が(c)を既定で疑う構造だった
- 一般化ルール (次ループの指示文としてそのまま使える形で): 実機検証で「効いていない」を確定する前に、①document計装リスナーでイベントの実到達先を確認し、②1マクロタスク後に再計測する。ヒットテストはmousedown前後の2回（hit1/hit2）行い、フォーカス由来のオーバーレイ（サジェスト等）の介在を検出する。preview_clickが別要素に着弾したら既知のDPRズレ（フォールバック=elementFromPoint＋座標付き合成イベント列dispatch。blurはmousedown後に明示的に発火させ実イベント順序を再現する）
- 反映先: loop prompt（既存「実機検証の規律」の運用詳細。preview_click DPRズレは既知entry〔mobile-ux-feedback〕のフォールバックルールが機能した再発）
- 発生回数: 1 回目

## 2026-07-03 M7後半 レビュアーのファイル読み落とし裁定 [GOOD]

- 事象: T42レビューのLow-2「StepPhotoTileコメント『mobile 76px』に対し縮小mediaが無い」を、impl委譲で「コメントを実態に合わせよ」と指示したが、implが「480px縮小mediaは実在しコメントは正しい＝対応不要」と差し戻した。セッションがgrepで直接確認し、implが正・レビュアーが480pxのmediaを読み落としていたと裁定
- 原因: レビュー結果を無条件に修正タスク化していた。レビュアーも見落とす（767pxのmediaは見たが480pxの2段目を見落とし）
- 一般化ルール (次ループの指示文としてそのまま使える形で): レビュー指摘を修正委譲する前に、Low含め「指摘の事実前提」をセッションがgrep/Readで1次確認する。implやレビュアーが事実認識で食い違ったら、多数決でなくセッションが原典（コード）を直接確認して裁定する。修正不要の指摘は「対応不要＋根拠」を記録してクローズ
- 反映先: loop prompt → **CLAUDE.md 昇格済み**（「委譲・レビューの規律」— 2026-07-05 mobile-home-overflowで同根2回目: タスク前提の修正案〔hiddenInputをvisually-hidden化せよ〕が事実と異なり〔全5ファイルで実装済み〕、セッションの1次確認〔375px実機再現→computed style→カスケード追跡〕が真因=HomePage `.actions > * {width:100%}` の同詳細度上書きを特定。前提のまま委譲していれば無効な修正を出荷していた）
- 発生回数: 5 回目（2026-07-07 S1 ST-08レビューR2のLow「コメントの関数名がresolveTechniqueLabelは誤記・実関数はresolveTechniqueName」は**事実前提が逆**〔Grepで実関数名resolveTechniqueLabelが正・コメントは正しい〕。1次確認で却下し無効な修正を回避。R1のHigh指摘は前提正確で採用＝指摘ごとに前提検証する運用が両方向で機能／2026-07-07 S0 ST-01レビューでMedium 2件とも事実前提が不正確〔「dist混入で3 error実機確認」=再現不能・dist内.tsなし実測0 error／「prettier --check src はexit 0偽PASS」=実際はexit 2の明示エラー〕。1次確認で切り分け、対応の妥当な部分のみ採用〔ignores衛生化・ゲートコマンド更新〕。read-onlyレビュアーの「実機確認済み」という主張自体を疑う根拠にもなった／2026-07-05 mobile-home-overflow で「タスク前提の事実誤り」を1次確認が捕捉 → 昇格／2026-07-05 crop-touch-scroll でセッション自身が委譲前提「bodyロックはPartEditorPageに実在」を記憶ベースで書き（実在はRecipeOverviewPage.tsx:75-125・方式もoverflow:hidden）1次確認を怠った。implのRead確認が食い違いを差し戻し実害なし — 昇格済みルールはsubagentの前提だけでなく**セッション自身が委譲プロンプトに書く事実**にも適用すること）

## 2026-07-04 M8前半 共通フックのテストが呼び出し元のマウント形態を代表せず（実機検証が捕捉） [BAD]

- 事象: useFocusTrapの復帰フォーカスが「open===false時のeffect」実装だったため、条件付きマウント（`{cond && <Dialog/>}`＝閉じるとアンマウント）のPartReviewDialog/ShareDialogでは一度も発火しなかった。ユニットテスト（propトグルのみ）と静的レビュー2ラウンド（R1/R2 PASS）を素通りし、出口実機検証のフォーカス追跡（activeElement確認）が検出。open時effectのクロージャcapture＋cleanup復帰へ修正しR3 PASS
- 原因: 共通フックの委譲プロンプトとテストが「適用先5ダイアログのマウント形態が2種類ある（propトグル維持 vs 条件付きマウント）」ことを明示せず、テストは片方の形態しか再現しなかった。cleanupセマンティクス（open→false とアンマウントの両方で発火）に乗らない実装は片肺になる
- 一般化ルール (次ループの指示文としてそのまま使える形で): 共通フック/共通部品を複数の呼び出し元へ適用する委譲では、design時に全呼び出し元のマウント形態（propトグル・条件付きマウント・portal）をGrepで列挙してプロンプトに明記し、テストに各形態を最低1ケースずつ含めさせる。ライフサイクル依存の後始末（フォーカス復帰・リスナー解除・ロック解除）はeffectのcleanupに置く（アンマウント経路を自動でカバーする）
- 反映先: loop prompt（「検証が実利用を代表していない」系の既存昇格ルールが実機側で機能した実証でもある）
- 発生回数: 1 回目

## 2026-07-04 M8前半 委譲プロンプトの過剰包括な禁止指示が誤解を生んだ [BAD]

- 事象: implへの「git操作は禁止（読み取り含め一切実行しない）」指示に対し、implが裁定材料として`git diff`（読み取りのみ）を使用し、完了報告で「抵触の可能性があるため自己申告します」と混乱を報告した。実害なし
- 原因: 並行委譲時のgit事故防止（lessons既存entry）の意図は「状態変更の禁止」だが、禁止を読み取りまで広げたことで、implの自己検証（自分の差分確認）という正当な手段と衝突した
- 一般化ルール (次ループの指示文としてそのまま使える形で): impl委譲プロンプトのgit禁止は「状態変更操作（add/commit/stash/checkout/reset/push等）の禁止」と書き、読み取り（status/diff/log）は「自己検証用に使用可」と明示する。禁止指示は意図（守りたい不変条件）が伝わる粒度で書き、手段を過剰に包括しない
- 反映先: loop prompt
- 発生回数: 1 回目

## 2026-07-04 T43④実機FBループ オーバーレイのタップ素通しは複数原因が積層する（段階検出をヒットテスト規律が支えた） [GOOD]

- 事象: 新設MarkdownCopyFallbackDialogのタップ素通しを実機ヒットテストが検出→z-index(100<シート200)を修正しても素通し継続→再ヒットテストが第2原因（.mobileRootのpointer-events:none継承）を検出→ラッパー適用で解消。さらに後続検証で第3の遮蔽要因（手動✕まで残存するエラートーストz:1000が下のボタンを覆う）も同じ規律が検出した。静的レビュー2ラウンドはいずれも検出できないクラスの欠陥だった
- 原因: オーバーレイの操作不能は「z序列」「pointer-events継承」「別オーバーレイの残存」など独立な原因が積層し得る。1原因を直して安心すると次の原因がマスクされたまま出荷される
- 一般化ルール (次ループの指示文としてそのまま使える形で): オーバーレイのタップ素通しを修正したら、同一シナリオのelementFromPointヒットテストを修正後に必ず再実行する（1原因の修正で確認を終えない）。新規ダイアログを既存ツリーへ差し込む委譲プロンプトには、レンダー先の暗黙前提（pointer-events継承・z序列・リフトアップ理由）を明示する。検証セッションではヒットテスト前に残存トーストを掃除し、エラー通知はダイアログと二重にしない設計を選ぶ
- 反映先: loop prompt（CLAUDE.md昇格済み「出口実機検証のヒットテスト」規律が機能した実証。委譲プロンプトへの暗黙前提明示は新規の運用ルール）
- 発生回数: 1 回目（z序列単独はM6で昇格済み・今回は「積層」の相が新規）

## 2026-07-04 noteMDクリップボード user activation依存APIはpreview検証で代表できない [BAD]

- 事象: T43④ループでnoteMDのクリップボード経路を「preview実機検証済み」としたが、本番iPhoneで「何も起きない」が再発。previewのclipboardは即座にNotAllowedError拒否（＝拒否経路のみ通る）な上、合成click()にはuser activationがなく、実iOSで起きる「writeTextのPromiseが解決も拒否もせずハングする」挙動はpreviewでは原理的に発生しない。結果分岐依存の実装はハング時にどのフィードバックにも到達しなかった
- 原因: user activation・OS権限に依存するAPI（clipboard・Web Share・通知等）は、previewの権限モデルと合成イベントが実機と本質的に異なる。「previewで動いた/失敗経路を見た」は実機の全経路をカバーしない
- 一般化ルール (次ループの指示文としてそのまま使える形で): user activation依存API（clipboard/share/通知）を使う機能は、①結果分岐に依存しない設計（タイムアウト付き多段フォールバックで、どの経路でも必ず可視フィードバックへ到達する）を最初から要求仕様に含め、②preview検証の限界を出口レポートに明示して「ユーザー実機確認が必須の項目」として引き渡す。ハングの再現テストはnever-settling Promiseスタブで書く
- 反映先: loop prompt
- 発生回数: 2 回目（2026-07-05 crop-touch-scroll: touch-action: none が全対話要素に付いていても実iOSでクロップドラッグが背後スクロールに吸われるユーザーFB。previewの合成イベントでは実タッチのスクロール競合を原理的に再現できず「preview検証済み」が実機を代表しなかった同根2例目。対処も同型=結果分岐に依存しない多段防御〔bodyスクロールロック＋非passive touchmove preventDefault＋overscroll-behavior〕 → **CLAUDE.md昇格済み**〔実機検証の規律へ「実機入力・権限依存挙動はpreviewで代表できない」を追加〕）

## 2026-07-05 i18n-4言語 CSS言語特例セレクタが新言語をカバーせず（全ブレークポイント×全言語の幾何検証が捕捉） [BAD]

- 事象: fr/de/it/es追加で、和文gloss用トラッキング（0.3〜0.4em字間）を解除する`:lang(en)`特例セレクタ（3ファイル: tagline/heroGloss/dividerLabel）が新言語に適用されず、ドイツ語375pxでヘッダーが471pxへ膨張（タグライン309px）する実害。静的レビュー2ラウンド（翻訳QA・a11y・z序列を観点指定）とselfcheckを素通りし、出口実機検証の「全ブレークポイント×全言語」幾何検算が捕捉。あわせてit/esのapp.tagline未翻訳（en文面コピー・ja≠enなので据え置きルール対象外の翻訳漏れ）も同検証で発覚。副作用の教訓: 意匠キー差し戻しのsed逐語置換がフランス語のエリジオン（de l'étape→de l'STEP）を壊し、R2レビューが捕捉（機械置換は言語文法を知らない）
- 原因: ①計画§7設計メモに「第3言語でglossをどう扱うかはデザイン判断」と明記されていたのに、design時にgloss関連の**既存CSSの言語分岐（:lang特例）をGrepで洗い出さず**、委譲プロンプトにも含めなかった（データ層のみのタスクと誤認）②新言語追加は「訳文の正しさ」だけでなく「既存コードの言語条件分岐（:lang・lng===比較・Intl系）の網羅」を要する
- 一般化ルール (次ループの指示文としてそのまま使える形で): 対応言語を追加するタスクのdesignでは、着手前に `:lang(`・`i18n.language`・`lng ===`・`Intl.` をGrepして**言語で分岐する全箇所を列挙**し、新言語がどちらの分岐に入るべきかを委譲プロンプトへ明記する。訳文の逐語置換修正（sed等）は置換後に前後の冠詞・格変化が壊れていないかを同一レビュアーの観点に含める。多言語UIの出口検証は「全ブレークポイント幅×全言語」で最長言語（de/fr）の収まり幾何（右端≤viewport）を検算する
- 反映先: loop prompt（「検証が実利用を代表していない」系の既存昇格ルールが機能した実証。言語分岐Grepは新規の運用ルール）
- 発生回数: 1 回目

## 2026-07-05 crop-ui-feedback overflow:hiddenに依存する既存効果を特定してからvisible化する2層分離design [GOOD]

- 事象: 「ハンドルがクリップされる」FBへの自然な対処（overflow:visible化）が、暗転のbox-shadow 0 0 0 9999pxが同じoverflow:hiddenのクリップに**依存して成立している**ことをdesign時のRead/1次確認で特定できたため、暗転レイヤー（frame内クリップ・非対話shadeRect）と操作レイヤー（overlay overflow:visible・cropRect＋ハンドル）の分離を最初から委譲プロンプトに指定し、1委譲・レビューR1 PASS（C0/H0）で成立した。レビューは座標契約（frameRef移管先stage box=旧frame border-boxの厳密一致）まで独立検算した
- 原因: 「1つのCSSプロパティ（overflow:hidden）が複数の役割（ハンドルのクリップ=バグ・暗転の閉じ込め=仕様）を同時に担っている」ことを、変更前にそのプロパティに依存する全効果を列挙して発見した
- 一般化ルール (次ループの指示文としてそのまま使える形で): overflow・z-index・position等の「文脈を作るCSSプロパティ」を変更するタスクのdesignでは、変更前に**そのプロパティに依存している全ての既存効果**（クリップ・スタッキング・containing block）を列挙し、壊れる効果があれば層分離（役割ごとに要素を分ける）を委譲プロンプトへ指定する。付記: elementFromPointの境界プローブは矩形端+1pxだと小数丸めで外れることがある（+2px内側を使う。「効いていない」誤判定の新しい相）
- 反映先: loop prompt（M6 z序列・T44ネストルート化と同族「暗黙の前提の無効化」を、事後検出でなく事前designで回避できた実証）
- 発生回数: 1 回目

## 2026-07-05 photo-crop schemaVersion昇格時にlistRecipes（一覧経路）のマイグレーション欠落でHome全滅 [BAD]

- 事象: schemaVersion 1→2（photoCrops必須フィールド追加）の実装で、lazy migrationがloadRecipe（個別ロード）にしかなくlistRecipes（一覧）は生レコードを返す設計の穴が、B-3のRecipeCard `recipe.photoCrops[...]` 直アクセスで顕在化。v1文書が残る実DBでHomeを開くとRecipeCardがthrowしアプリ全体がアンマウント（エラーバウンダリなし・body空白化）のCritical。ユニットテスト1092件は全緑（fixtureが全てv2だったため）で、B-4出口実機検証のみが捕捉した。根治=listRecipesへ同一マイグレーションパイプライン適用（in-memoryのみ書き戻しなし・破損/未来バージョンは可用性優先でスキップ+console.warn）
- 原因: ①「フィールド追加はスキーマ+マイグレーション登録で完結」という思い込みで、**DBの全読み出し経路**（個別・一覧・エクスポート・複製・リマインダー）がマイグレーションを通るかを設計時に検算しなかった ②v1→v1恒等の時代は経路の穴が無症状で潜伏していた ③テストデータが現行バージョンのみで「旧バージョン文書が混在するDB」を代表していなかった
- 一般化ルール (次ループの指示文としてそのまま使える形で): schemaVersionを上げるタスクのdesignでは、`db.recipes`（等の対象テーブル）を読む全経路をGrepで列挙し、各経路がマイグレーションを通ることを委譲プロンプトの完了条件に含める。出口実機検証は**旧バージョン文書が混在するDB**（実機に残る旧データ or 生put投入）で全画面を開く。新フィールドへの最初の直アクセスが「潜伏していた経路の穴」の発火点になると知れ
- 反映先: **CLAUDE.md 実機検証の規律へ追記**（データバリエーション行に「旧schemaVersion文書の混在」を追加。「検証が実利用を代表していない」ファミリーの新相=バージョン混在）
- 発生回数: 1 回目

## 2026-07-05 photo-crop 座標編集UIの保存値が契約座標系とズレる（レターボックス見落とし）を実機幾何目視が捕捉 [GOOD]

- 事象: PhotoCropDialogの正規化座標が`.frame`基準で、imgの`object-fit: contain`レターボックス（縦長写真×4:3フレームの左右余白）込みの値が保存されており、契約（cropRectSchema「元画像に対する正規化矩形」・B-3のcomputeCoverSourceRectの前提）とズレていた。レビュー2ラウンドは観点に「clientX→正規化座標変換の正しさ」を含めても素通り（変換式自体は正しく、**座標空間の同一性**が問題だったため）。出口実機のスクリーンショット目視「クロップ矩形が写真の外の余白まで広がっている」が捕捉。修正（フレーム比=画像比化）も、初版のaspect-ratio+max-*のみ指定が実機でフレーム0px潰れ（absolute子は内容量に寄与しない・jsdomはレイアウトなしで検出不能）→width明示で確定、と実機幾何検証が2連続で機能した
- 原因: 座標変換の「式」と「空間」は別の検証対象。静的レビューは式は検算できるが、object-fit等のCSSレイアウトが作る表示座標空間と契約座標空間の乖離はレンダリングなしでは見えない
- 一般化ルール (次ループの指示文としてそのまま使える形で): 座標編集UI（クロップ・アノテーション・ドラッグ配置）の出口検証では、①編集対象の表示要素と座標基準要素のboundingClientRectが一致すること（レターボックス・border・paddingの介在なし）②保存値→別ビューでの再表示の幾何が数値一致すること、を実データで照合する。CSSのaspect-ratioでサイズを導出する要素は「内容量に寄与する子がいるか」を確認し、absolute子のみなら幅か高さを明示する（jsdomでは検出不能・実機必須）
- 反映先: loop prompt（実機検証の規律の運用詳細。「生成物は中身まで見る」の座標系版）
- 発生回数: 1 回目

## 2026-07-05 share-card-portrait マージ済みPRのブランチへ追加pushしコミットが宙に浮いた [BAD]

- 事象: PR #26作成後にユーザーから追加指示（A-3 summary動的配分）を受け、「同一PRに積む」判断で同ブランチへ追加コミットをpushしたが、その時点でPR #26は既にマージ済みだった（ユーザーのマージとセッションの追加作業が並行）。pushはエラーなく成功し、コミットはどのPRにも属さないままブランチ上に浮いた。ユーザーの指摘（「#26はマージ済みだよ。ちゃんとPRしてるか確認して」）で発覚し、同ブランチから新規PR #27を起票して回収（マージ後ブランチのためdiffはA-3のみでクリーン）
- 原因: 「PRに追加コミットを積む」操作の前提（PRがまだopenである）を確認しなかった。git push はマージ済みブランチへも成功するため、操作自体からは異常が見えない。ユーザーが非同期にマージし得ることはPR運用の常態なのに、セッション内の「PR作成済み」というスナップショット認識を陳腐化する前提で扱わなかった（git初期化事故と同根の「開始時スナップショットの陳腐化」の相）
- 一般化ルール (次ループの指示文としてそのまま使える形で): 既存PRへ追加コミットを積む前に必ず `gh pr view <N> --json state` でopenであることを確認する。MERGEDなら新規PRを起票する（同一ブランチからの続きpushでもマージ後コミットだけがdiffになるので回収は容易）。push後は `gh pr view --json commits` で対象コミットがPRに入ったことを確認してから完了報告する
- 反映先: loop prompt（git系スナップショット陳腐化の2例目。git状態確認ルールにPR状態を追加）
- 発生回数: 1 回目（同根「セッション開始時スナップショットの陳腐化」〔2026-07-02 git初期化事故〕と合算で2回目 — 次回同種でCLAUDE.md昇格）

## 2026-07-05 share-card-portrait DL生成物の実機検証はcreateObjectURLフックでBlob本体を捕獲する [GOOD]

- 事象: DLファイル名と実ピクセルの実機検証で、`anchor.click`フックで捕獲したblob URLが表示前に死んでいた（`downloadBlob`がクリック直後に`URL.revokeObjectURL`する正しい実装のため）。`URL.createObjectURL`を先にフックしてBlob本体を捕獲→FileReaderでdataURL化→画面表示に切替えたところ、ファイル名4系統・寸法1080×1350・実ピクセル目視まで一気通貫で検証できた。あわせて「式ベースのレイアウト定数」（SUMMARY_STEP_LIST_AREA_HEIGHT=CARD_HEIGHTからの導出式）だったため、寸法変更が定数2行の変更で全収容計算に自動追従し、impl 2委譲・レビュー1ラウンドPASSで完了した
- 原因: objectURLはDL系ヘルパーが後始末でrevokeするのが定石であり、URL文字列の捕獲では生成物検証に使えない。レイアウト定数を直値でなく導出式で持つ設計は寸法変更系タスクの変更面を最小化する
- 一般化ルール (次ループの指示文としてそのまま使える形で): DL生成物（画像・ファイル）の実機検証は`URL.createObjectURL`をフックして**Blob本体**を捕獲し、dataURL化して表示・目視する（anchor.hrefのblob URLはrevoke済みで使えない前提で書く）。ファイル名検証は`HTMLAnchorElement.prototype.click`フックで`download`属性を読む（実DLも抑止できる）。レイアウト定数は直値でなく基準寸法からの導出式で定義させる
- 反映先: loop prompt（実機検証の規律の運用詳細）
- 発生回数: 1 回目

## 2026-07-05 help-faq リポジトリ資産化するスクショ素材はPlaywright別プロファイル＋UIインポートで撮影・段階委譲はenプレースホルダーでゲート常緑 [GOOD]

- 事象: /helpページのスクショ素材12枚を、①preview_screenshotがファイル保存不可という制約に対しscratchpadへPlaywrightを導入（プロジェクトpackage.json無改変）②別プロファイルのIndexedDB空問題は黒狼JSONのUIインポートで実データ再現③全枚数目視ゲートが撮影バグ2件（インポート後の自動遷移でHomeショットがOverviewになる・ストレージ警告バナー写り込み）を出荷前に検出、の3段で完遂した。また、i18n全7ロケール構造一致テストがある環境で「ページ実装（ja/en）→翻訳（5言語）」を順次委譲する際、先行タスクに「残り5言語へenプレースホルダー投入」までを含めたことで、委譲間の全ゲートが常緑のまま保てた
- 原因: 撮影素材の生成は「ファイルとして保存できる経路」「実データの再現手順」「中身の目視ゲート」の3点をdesign時に確定していたため、手戻りが撮影スクリプトの修正2回で収まった
- 一般化ルール (次ループの指示文としてそのまま使える形で): 生成素材（スクショ・画像・PDF）をリポジトリ資産化するタスクは、design時に①ファイル保存可能な生成経路（preview_*は不可・Playwright等）②素材に写る実データの再現手順（UIインポートが最も仕様適合）③全点数の目視ゲート、を決めてから着手する。再生成スクリプトはdocs/tools/へ保全する（scratchpadは揮発）。全ロケール構造一致テストがある環境でキー群を段階委譲するときは、先行タスクの完了条件に「未翻訳ロケールへのenプレースホルダー投入」を含め、委譲間でゲートを赤にしない
- 反映先: loop prompt
- 発生回数: 1 回目

## 2026-07-05 ko対応 言語分岐Grepルールが機能＋CJKスライスフォントの実ロード判定手法 [GOOD]

- 事象: ①前ループ（fr/de/it/es）で昇格した「言語追加時は `:lang(`・`i18n.language`・`lng ===`・`Intl.` をGrepして全分岐を列挙」ルールをdesign段階で適用した結果、(a)Xカウンタがホワイトリスト方式でハングルは既に重み2=本体無変更と事前確定 (b):langトラッキング解除3ファイルへのko追加（ハングルへの字間流用は禁忌）を委譲プロンプトに最初から明記でき、前回の「実機検証まで到達する見えないCSSバグ」を未然に防いだ。レビューR1もM1（訳語1件）のみでR2 C0/H0/M0/L0 ②新手法: Google FontsのCJKスライス配信では `document.fonts.check()` が実描画中でもfalseを返す（unicode-range分割の全face一括判定のため）。実ロード判定は「canvas measureTextで対象フォント名 vs 明示フォールバックの幅差」＋「loadedステータスのスライス数」＋「スクリーンショット目視（明朝/ゴシックの字形差）」の3点で確定できた。低頻度音節の一時注入でオンデマンドスライス取得も検証可能
- 原因: 予防が効いたのは「同種タスクの直近の失敗をルール化し、次の同種タスクの入口で機械的に適用した」ため。フォント判定は check() の仕様（全faceロード済みのみtrue）がスライス配信と相性が悪いことを実測で特定した
- 一般化ルール (次ループの指示文としてそのまま使える形で): CJK Webフォント（スライス配信）の実機検証では `document.fonts.check()` を合否判定に使わない。①canvas measureText の幅差（対象フォント vs 明示ゴシック vs generic）②`[...document.fonts]` の loaded スライス数 ③スクリーンショットの字形目視、の3点で判定する。ユーザー入力由来の任意文字は低頻度文字を一時注入してオンデマンド取得を確認する
- 反映先: loop prompt（言語分岐Grepルールの有効性実証＋フォント検証の新定石）
- 発生回数: 1 回目

## 2026-07-07 S0-monorepo ゲート定義が変わるマイルストーンでベースラインを旧ゲートでしか測らず、implが既存問題の切り分けに沈んだ [BAD]

- 事象: S0でprettierゲートが `--check src` → `--check apps`（tests/等を含む）へ広がるのに、セッションのベースライン測定は旧ゲート（--check src）のみで行った。移動後に新ゲートが tests/exporters.snapshot.test.ts の未整形（ベースラインから存在・旧ゲートの死角）を検出し、implは「自分の変更が原因か既存問題か」の切り分け調査（git worktree作成まで）に沈み、その最中に600sストールで切断された。セッションが残骸判定時に同じ切り分け（baseline worktreeで--check実行＋バイト一致diff）をやり直して確定した
- 原因: ベースラインは「これから合格判定に使うゲート」で測らないと、新ゲートが顕在化させる既存赤が委譲先のノイズ（自責か既存かの調査コスト）になる。ゲートのスコープ拡大は「既存の死角の顕在化」を必ず伴う
- 一般化ルール (次ループの指示文としてそのまま使える形で): マイルストーンでゲート定義（コマンド・対象パス）が変わるタスクは、着手前のベースライン測定を**新旧両方のゲート**で行い、新ゲートで赤になる既存問題を委譲プロンプトに「既知・スコープ外（またはglue対象）」と明記して渡す。subagentに「既存問題かどうかの切り分け」をさせない（切り分けはセッションがベースラインで先に済ませる）
- 反映先: loop prompt（「検証が実利用を代表していない」ファミリーの新相=ベースラインが新ゲートを代表していない）
- 発生回数: 1 回目

## 2026-07-07 S0-ST03 計画時に用意したフォールバックが最大リスクの現実化を即日吸収した／PaaSの失敗はローカル再現で確定させてから引き渡す [GOOD]

- 事象: 計画§8-6で「最大の移行リスク」と明記していたPages monorepoビルド（Root directory=apps/codex）が実際に失敗した。①GitHub check-runでビルド失敗を確認（本番はPagesの直近成功デプロイ配信仕様で無傷）②セッションがフレッシュクローン＋apps/codex配下installで**同一エラーをローカル再現**（真因確定: サブディレクトリからのnpm installはルートdevDepsを入れない→tsc -bがsrc内*.test.tsxの型検査でvitest不在）③計画済みフォールバック（Root=/＋Build command直接指定）を**ユーザーへ渡す前に同クローンで検証**（npm ci→build -w exit 0）④切替後、本番バンドルハッシュとローカルビルドの完全一致で「新デプロイの実体」を確認。ユーザーとの往復は2回で完了
- 原因: リスクの高い外部環境変更（PaaS設定）に対し、(a)計画段階でフォールバックを具体的コマンドまで用意していた (b)「反映された」の判定を全ルート200で済ませず、check-run（デプロイの成否）とバンドルハッシュ（配信物の同一性）まで見た（アプリソース不変の移行では旧デプロイの生き残りも全ルート200を返すため、ルート疎通は新旧を区別できない）
- 一般化ルール (次ループの指示文としてそのまま使える形で): PaaS/CI環境のビルド失敗は、ダッシュボードログを待つ前にセッションがフレッシュクローンで同一条件（実行ディレクトリ・install経路）を再現して真因を確定し、修正案も同クローンで検証してからユーザーに引き渡す。デプロイ反映の確認は「ルート疎通200」で止めず、①デプロイ自体の成否（check-run/API）②配信物の同一性（バンドルハッシュとローカルビルドの突き合わせ）まで行う（ソース不変の移行では疎通だけだと旧デプロイと区別できない）。環境変更系タスクの計画には具体的コマンドレベルのフォールバックを必ず用意する
- 反映先: loop prompt（§1.3-4フォールバック設計の有効性実証。ST-12のscriptorium新設は本採用方式を計画v1.1に反映済み）
- 発生回数: 1 回目

## 2026-07-07 S1-recipe-core 禁止パターン検査は「フィールド列挙」でなく「表示層に到達する文字列経路の列挙」から設計する（レビュー観点明示がHighを実装前検出） [GOOD]

- 事象: ST-08のpublishedRecipeStrictSchemaで、implは自由テキストの禁止パターン検査（URL・`<`・`javascript:`）を「title・palette name/brand・tool name・part name・technique.label」のフィールド列挙で実装し、`technique.presetKey`を素通しした。presetKeyは「マスタキーのenum的な値」という印象だが、実際はz.string()自由入力で、`resolveTechniqueLabel`分岐③（マスタ外presetKeyの旧データ防御）が**そのまま表示文字列として返す**ため、labelと同じ表示先に流れる第2の経路だった。レビュー委譲プロンプトに「漏れているフリーテキストフィールドがないか実スキーマで検算」「拒否パターンの回避耐性」を観点として明示していたため、レビューR1がHighとして実装前（ストア到達前）に検出。修正はlabelと同一検査の適用＋回避耐性テスト8件で1バッチ・R2 PASS
- 原因: 検査対象の設計を「人間が自由入力するフィールド」の直感で列挙すると、「システム由来に見えるが実は無制約な文字列」（presetKey・将来的にはID系・enum風string）が漏れる。防御の正しい単位はフィールドの出自でなく**シンク（表示層・href・実行文脈）に到達するか**
- 一般化ルール (次ループの指示文としてそのまま使える形で): 公開スキーマ・入力境界の禁止パターン検査をdesign/実装するときは、「自由テキストのフィールド列挙」でなく「表示層・URL文脈に到達する全string経路」をレンダリング側コード（resolve系関数・表示部品）から逆引きで列挙して検査対象を確定する。z.enumでない`z.string()`は名前がキー風でも自由入力として扱う。この種のタスクのレビュー観点には「実スキーマでの検査対象フィールドの網羅検算」「大文字・スキーム変種の回避耐性」を必ず明示する（本ループで実際にHighを事前検出した実証）
- 反映先: loop prompt（S3のST-14/18=API境界・S2のUI層でも同じ逆引き列挙を適用）
- 発生回数: 1 回目

# state — ループの背骨

セッションは毎ループの入口で本ファイルを Read し、出口で更新する。
モデルはセッションを跨ぐと忘れるが、このファイルは忘れない。

最終更新: YYYY-MM-DD (loop: <名前>)

## 完了

- (例) YYYY-MM-DD: テスト/lint 基盤の構築 — pytest + ruff、以後の /goal 条件を exit code で判定可能に

## 進行中

- (なし)

## 次の候補 (優先順)

1. (ここに backlog を書く)

## 決定事項 (変更には理由が要る)

- (例) 通貨計算は Decimal を使う (float 禁止)

## 申し送り (次セッションの自分へ)

- (なし)
